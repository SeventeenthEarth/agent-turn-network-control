# E2E Tests

BOOTS-001 establishes this directory for future isolated E2E tests.

E2E tests must fail closed or skip explicitly unless a disposable Hermes profile/home and dedicated test target are configured. They must never use the current running Hermes profile, production Discord rooms, or user credentials by default.
