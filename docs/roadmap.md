# Release v1 Roadmap

## Roadmap rule

This roadmap is the SSOT for Go control epic/task status, sequencing, and evidence pointers. Python Hermes plugin roadmap status lives in `../../agent-turn-network-plugin/docs/roadmap.md`.

Other control docs may cite task IDs for context, but current task status must be read from this file. `docs/todo/implementation-decomposition.md` remains a decomposition/reference document, not the status owner.

Roadmap tasks must be **capability-sized**, not file-sized. Each row should normally be large enough for one Kkachi/KAH task contract, one implementation lane, tests, docs/evidence update, role review, and one commit. Split a task only when dependency order, approval gate, failure domain, or reviewer specialty is materially different.

Epic IDs are five-letter uppercase English slugs. Task IDs are derived from the epic ID as `{EPIC}-001`, `{EPIC}-002`, and so on. Explicit user-approved exceptions include `ATN`, `RUNFIX2`, `RUNFIX3`, and the `NEXFIX` epic whose requested task id prefix is `NEWFIX-*`. For cross-repo epics, control and plugin share one global sequential task stream and cite tasks as `control/<task-id>` or `plugin/<task-id>`; repo-local numbering gaps are expected. Status values are compact operator-facing values: `planned`, `in_progress`, `implementation_complete/review_pending`, `completed`, or `blocked`.

## BOOTS — Bootstrap

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| BOOTS-001 | Control scaffold and local gates | completed | Create `go.mod`, `cmd/atn-control`, `cmd/atn-controld`, `internal/`, Makefile target contract, docs guardrails, and binary help smoke tests that pass without external resources. |

## REGST — Registry/security

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| REGST-001 | Registry authority | completed | Implement deterministic data-home resolution, strict registry schema, fail-closed permissions/symlink/TOCTOU checks, reserved principal rejection, wrapper/env validation, per-session registry snapshot, and `registry validate/show` CLI tests. |

## STORE — Storage/event SOT

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| STORE-001 | Event-store append | completed | Implement safe session directories, session metadata, registry snapshot metadata, canonical `channel.jsonl` event append, and surface/linked-authority evidence fields with unit and integration coverage. |
| STORE-002 | Projection and replay | completed | Implement SQLite projection as a rebuildable cache, deterministic replay/rebuild, `storage verify`/`storage rebuild-projection`, doctor storage health, and projection/replay/CLI tests. |

## DAEMN — Daemon/CLI/protocol

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| DAEMN-001 | Daemon and CLI commands | completed | Implement `atn-controld` lifecycle, local command transport, canonical `atn-control` commands, status/doctor/health, structured JSON errors, and stable exit categories verified through CLI integration tests. |
| DAEMN-002 | Stream and conformance | completed | Implement stream replay/follow/cursor acknowledgement, active-session lock, version/feature endpoint, command/event/error/stream fixtures under `testdata/conformance/`, and plugin-compatible protocol checks. |

## RUNRT — Runtime/runner

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| RUNRT-001 | Member runtime and runner | completed | Implement member runtime loop contract, bounded `hermes-agent` runner adapter, wrapper accounting, fake-runner tests, and operator docs. |

## DELEG — Delegation/review

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| DELEG-001 | Delegation and review gates | completed | Implement delegation lifecycle, review request/response gates, blocked/resume handling, CLI/E2E fake coverage, and audit evidence. |
| DELEG-002 | Delegation/review conformance fixture matrix | completed | Publish plugin-consumable delegation/review command and structured-error fixtures for success, duplicate/idempotency, permission/error, retryable failure policy, and malformed-response handling so atn-plugin DELRV-2 can add failure coverage without inventing control-owned shapes. |

## COUNC — Council/consensus

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| COUNC-001 | Council and consensus | completed | Implemented local council lifecycle commands, speaker/moderator policy, voting/consensus state, static conformance fixture handoff, and council tests. |

## TRANS — Transcript/distribution

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| TRANS-001 | Transcript and distribution | completed | Implemented golden transcript/export rendering, install/distribution docs, plugin handoff checks, and operator acceptance evidence. |

## RELIA — Reliability/release

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| RELIA-001 | Reliability and release acceptance | completed | Implement observability, disaster recovery, corruption handling, replay rebuild, full Release v1 acceptance suite, and release readiness evidence. |

## LTRAN — Live transport control compatibility

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| LTRAN-001 | Control live transport SOT and mapping | completed | Record the control companion SOT, roadmap/docs cross-links, daemon/CLI/plugin/member-runtime ownership split, and epic-boundary repo handoff rule. |
| LTRAN-002 | Confirm daemon compatibility reads | completed | Added/confirmed explicit `version.read`, `status.read`, `diagnostics.read`, bounded `stream.replay` follow, `stream.status`, `stream.ack`, and concrete command-path compatibility evidence with conformance fixtures/checks; operator `status`/`health` remain concise and no live-local proof is claimed. |
| LTRAN-003 | Prove CLI/daemon live-local support | completed | Proved disposable data-home CLI/daemon live-local support with daemon-backed `compat` reads, stream replay/follow/ack/status, `delegate.submit` write/idempotency, structured command-id conflict behavior, first color review, GLM Octo, post-Octo re-review, and local/cross-repo verification evidence; no production activation or plugin mutation is claimed. |

## MEMBR — Member runtime profile invocation

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| MEMBR-001 | Select member runtime pilot mode | completed | Selected main-agent mediated bounded runner invocation as the first disposable local proof before long-lived member runtimes, with real profile/wrapper identity, runner/session evidence requirements, fail-closed policy, and no role substitution. |
| MEMBR-002 | Prove selected participant invocation | candidate/isolated proof | Blue accepted an isolated fake-wrapper implementation proof that `speaker_selected` dispatches only the selected registry member through the bounded runner path and records success or durable failure evidence. Real-profile invocation, live daemon/profile activation, provider/gateway/auth/token mutation, and production readiness remain unproven and approval-gated. |

## SURFD — Surface delivery evidence contract

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| SURFD-001 | Define surface rendering evidence contract | completed/docs-only | Defines the daemon event fields, transcript/projection inputs, delivery evidence status, and failure/pending-follow-up semantics needed for visible speech/final-result rendering; Blue accepted the docs-only contract after ATN Red/Orange/Gray review. Runtime projection proof remains `control/SURFD-002`. |
| SURFD-002 | Prove delivery evidence projection | completed/local proof | Local transcript/export proof exposes speech renderability, finalization, unresolved/cancelled outcomes, and fail-closed `posted`/`failed`/`pending_followup`/missing delivery-evidence states for plugin-visible rendering tests; Blue accepted after ATN Red/Orange/Gray review. |

## ENSOT — Event/outcome visible-closeout SOT

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| ENSOT-001 | Council terminal outcome visible-surface SOT | completed/docs-only | Docs-only SOT gate for plugin `VISUX`: `draft_conclusion` and `consensus_vote*` are visible process milestones only, `council_finalized` / `council_unresolved` are durable terminal outcomes, and human-readable moderator closeout requires posted surface/projection evidence that points back to the terminal event. Accepted after ATN Red/Orange/Gray review and Blue synthesis; no plugin implementation, live Discord delivery, production activation, or commit approval is claimed. |

## ARGUE — Council argument graph and discussion quality

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| ARGUE-001 | Council argument graph SOT closeout | completed | Accepted/completed docs-only closeout for the control-owned SOT, docs index, docs map, implementation epic, and roadmap links for council discussion-quality argument graph work. Official review passed with Red `t_4a2e735f`, Orange `t_9f4b2b9c`, and Gray `t_b196d630`; Blue synthesis is satisfied by this closeout. This does not authorize protocol implementation, fixture publication, plugin changes, live Discord delivery, production activation, or `control/ARGUE-002`. |
| ARGUE-002 | Protocol shape and conformance fixtures | completed | Additive control protocol shape and plugin-consumable static conformance fixtures for `claims[]`, `stance_links[]`, contribution type, hand-raise `target_links[]`, and structured negative examples were accepted for bounded local scope under KAS/KAH run `run-20260615T145822Z-caab064cf550` after Red `t_e2ced3fc`, Orange `t_fd35e83a`, Gray `t_c9e20348`, Blue synthesis `t_ade91c69`, and final gate `evt-001437`. This does not claim runtime validation/scoring, transcript/export rendering, plugin implementation, live Discord delivery, production activation, commit/push beyond explicit approval, or readiness for ARGUE-003/004/005. |
| ARGUE-003 | Validation and moderator scoring hooks | completed | Local Stage 1 direct Codex SDK/app-server implementation completed under KAS/KAH run `run-20260615T181228Z-b79cfade404a`: daemon/storage validation, quality-required rejection, quality-warn diagnostics, and moderator graph-need scoring hooks have focused local test evidence, color review, Octo review, post-Octo re-review, final KAH gate, and commit approval. This does not claim plugin readiness, live readiness, production activation, or push approval. |
| ARGUE-004 | Transcript/export/projection preservation | completed | Local Stage 1 direct Codex SDK/app-server implementation completed under KAS/KAH run `run-20260616T073755Z-f2fe201156c7`: ARGUE relation evidence is preserved in transcript/export/projection surfaces with malformed relation diagnostics, focused storage/transcript/projection tests, first color review, Red R1 color-adjust, focused Red re-review, official KAB GLM Octo review, and post-Octo Red/Orange/Gray re-review plus Blue synthesis. This does not claim plugin readiness, live readiness, production activation, commit/push beyond explicit approval, or operator-visible pilot readiness; plugin JSON-string parsing and missing-field transcript UX conditions remain handoff/pilot gates. |
| ARGUE-005 | Control integration verification gate | completed | Local control-side integration verification gate completed under KAS/KAH run `run-20260616T132731Z-781418864c04`: plugin-contract compatibility, ARGUE conformance fixtures, validation/scoring regressions, transcript/export/projection preservation, and full local `make test` passed after observed `plugin/ARGUE-003` commit `3f0dc55`. This does not claim plugin roadmap cleanup, plugin/ARGUE-004, live-local pilot readiness, production activation, Discord delivery, profile/plugin install, gateway/auth/token/provider mutation, commit/push beyond explicit approval, or operator-visible pilot readiness. |


## ATN — Agent Turn Network public rename

ATN is the user-approved clean public rename to **Agent Turn Network**. ATN uses one five-task cross-repo sequence across the control and plugin repositories. This control roadmap records the full sequence for planning clarity; repo-qualified execution remains required. The rename has no public legacy aliases.

Live readiness, production activation, Discord delivery, package publication, hosted repository rename, push, and profile/provider/gateway/auth/token mutation remain separately approved scopes.

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| ATN-001 | ATN naming SOT and roadmap lock | completed/docs-only | Added control and plugin ATN naming SOT documents, recorded ATN-001 through ATN-005 in the roadmaps/docs maps, and locked the no-alias policy. Review consensus: Red `t_d43402f0`, Orange `t_6d6bb8e8`, Gray `t_7ebc9e1e`, Blue synthesis `t_8e348f72`. This does not rename code, binaries, packages, tools, live profiles, hosted repositories, or public release artifacts. |
| ATN-002 | Control public docs rename | completed/local-docs-proof | Rewrote control public docs, docs index/map surfaces, protocol wording, examples, operator-facing text, and roadmap status wording to ATN-only naming ahead of ATN-004, updated sibling workspace path references to the current `agent-turn-network-plugin` checkout, and removed superseded prior-rename references from the checked-in docs set. |
| ATN-003 | Plugin public docs rename | completed/local-docs-proof | Plugin-owned task rewrote plugin public docs, package/docs metadata, operator guide, bundled skill documentation, and local sibling workspace references to ATN-only wording ahead of ATN-005 with superseded prior-rename references removed from the checked-in docs set. |
| ATN-004 | Control code and binary rename | completed/local-proof | Renamed the control Go module, CLI binary, daemon binary, data-home/env/socket/protocol markers, daemon principal, conformance fixtures, tests, scripts, and Makefile surfaces to `atn-control`, `atn-controld`, `ATN_HOME`, `ATN_CONTROLD_PATH`, `~/.atn`, XDG `agent-turn-network`, and `atn-protocol-v1alpha0` with no aliases. |
| ATN-005 | Plugin package, tools, skills, and final guardrails | completed/local-proof | Plugin-owned task renamed the package/import/manifest/tools/bundled skills to ATN names, updated no-alias guardrails, and closed final cross-repo ATN compatibility proof. |

Every roadmap item must map to the Makefile target taxonomy in `docs/spec/testing-and-tooling.md` and to the phase dependencies in `docs/todo/implementation-decomposition.md`. For legacy repo-owned epics, active task transfer between this control repo and the plugin repo happens only at an epic boundary. For an accepted cross-repo epic such as RUNFIX, transfer happens at the globally ordered task boundary recorded in both repos' SOT tables. When a task ID is cited outside its repo-local roadmap, qualify it as `control/<task-id>` or `plugin/<task-id>`.


## RUNFIX — ATN council runner, activation, and discussion-quality remediation

RUNFIX is a cross-repo remediation epic using a single global task sequence across control and plugin. This control roadmap lists only control-owned rows; plugin-owned rows live in `../../agent-turn-network-plugin/docs/roadmap.md`.

Legacy pre-ATN artifact and label fragments in older RUNFIX rows are historical/provenance only, not current public ATN aliases.

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| RUNFIX-001 | Control remediation SOT and roadmap lock | completed | Accepted docs-only SOT lock for the control-side RUNFIX remediation contract, global cross-repo DAG, canonical readiness/fallback label taxonomy, selected-speaker runner/adaptor/ARGUE quality handoff boundaries, and roadmap/docs-map entries. Review evidence: Red `t_612b4d58`, Orange `t_c673aed4`, Gray `t_ce1b0c31`, focused Orange `t_131ea8c9`, focused Gray `t_7cec278f`, Blue synthesis `t_1bb67569`. This does not authorize RUNFIX-003..010 implementation or live readiness. |
| RUNFIX-003 | Selected-speaker member runtime dispatch | completed/local-control | Wired automatic selected-speaker dispatch so `speaker_selected` can start the selected member runner path without a custom harness. The local/control implementation preserves snapshot member identity, cursor/idempotency, canonical speech validation for selected-runner success, durable discard/failure diagnostics, and started-only replay/restart protection. Evidence does not claim live Discord delivery, production daemon activation, profile/provider/gateway/auth/token mutation, commit/push, or KAB `native_codex`. |
| RUNFIX-004 | Hermes adapter command contract and diagnostics | completed | Replaced the incorrect platform-delivery command assumption with an explicit response-generation command contract, preserved redacted invocation diagnostics, and prevented stale runner phase evidence. Historical implementation evidence remains local/control-only in control commit `0138b59` and KAH run `run-20260617T101645Z-1757e05ffbcf`; the current `completed` row is a later status normalization after focused re-validation, not a new readiness claim. |
| RUNFIX-005 | ARGUE and moderator quality gates | completed/local-control | Local/control Stage 1 implementation under KAH run `run-20260618T020120Z-fe2144618fe6` separates lifecycle status from `discussion_quality`, exposes ARGUE diagnostic counts and hard-warning codes, keeps `quality_required` fail-closed after the opening window, accepts `quality_warn` speech without text mutation or inferred durable links, and adds deterministic linked hand-raise `graph_need` counts. Local evidence passed `git diff --check`, focused storage/protocol/daemon/command tests, `make docs-guardrails`, `make check-plugin-contract`, `make test-prepare`, and `make test`; Red `t_1d5692f1`, Orange `t_388bb347`, Gray `t_6fb40282`, and Blue synthesis `t_1eb87c6b` accepted the bounded local-control closeout. This does not claim live Discord delivery, production daemon activation, profile/provider/gateway/auth/token mutation, plugin implementation/readiness, commit, push, or broad rollout. |

Plugin-owned `plugin/RUNFIX-006`, `plugin/RUNFIX-007`, and `plugin/RUNFIX-008` now have local implementation proof under plugin KAH runs `run-20260618T045937Z-2e173b8309f3`, `run-20260618T081811Z-23d10e2a4634`, and `run-20260618T092359Z-401d6e5bedc0`. RUNFIX-007 extends the pure/local `atn_discussion_activation_plan` with effective Discord eligibility evidence, eligible-only `allow_list_targets`, profile remediation, parent-channel proof state, thread-only proof rejection, fallback audit, and `live_readiness: false`. RUNFIX-008 extends the same pure/local planner and packaged operator guidance with participant ARGUE response evidence, ARGUE counts, selected-runner evidence, canonical `speaker_selected -> speech` linkage, and diagnostic-only fallback disclosure. `plugin/RUNFIX-010` completed under KAH run `run-20260618T112843Z-40b023a5d9c8` with bounded PASS_WITH_RISK parent-channel visible pilot evidence, final operator package, Discord-origin `live_visible_thread` default, unified non-visible override guardrails requiring supported `requested_output_mode` plus `explicit_non_visible_override=true` and non-empty `override_reason`, and final-report separation of lifecycle/visible-turn/profile-gateway/CLI-only evidence. This control roadmap does not convert those plugin-owned proofs into control live readiness, daemon startup authority, Discord delivery, production activation, profile/provider/gateway/auth/token mutation, or broad rollout readiness. `plugin/RUNFIX-015` now has local implementation proof under plugin KAH run `run-20260619T071526Z-7d2ba33b07d5`; `plugin/RUNFIX-017` has plugin-owned local implementation proof under plugin KAH run `run-20260619T101255Z-189d01ba8b8f`; `plugin/RUNFIX-019` has plugin-owned local implementation proof under plugin KAH run `run-20260619T214004Z-4d1e54b7304a` for registry membership/reconcile activation-planner guidance after `control/RUNFIX-018`. This control roadmap records the dependency boundary only.

| RUNFIX-009 | Integrated control smoke fixtures | local implementation proof | Control KAH run `run-20260618T102752Z-7d8ccfa584e4` adds an integrated daemon smoke fixture and transcript summary fix proving runner invocation, canonical `speaker_selected -> speech` linkage, ARGUE `quality_warn` diagnostics/hard-warning exposure, and transcript/export/projection closeout evidence from deterministic local events. Verification passed focused RUNFIX-009 daemon smoke, related selected-speaker/export tests, storage package tests, `git diff --check`, `make docs-guardrails`, `make check-plugin-contract`, `make test-prepare`, and `make test`; Red plan review `t_3e1a6ecd` accepted scope. This remains control-local proof only and does not claim live Discord delivery, production daemon activation, profile/provider/gateway/auth/token mutation, plugin/RUNFIX-010 execution, commit, push, or broad rollout. |
| RUNFIX-011 | Participant runtime readiness and attendance preflight | local implementation proof | Control KAH run `run-20260618T162156Z-419f3769f2cc` adds derived participant runtime readiness from durable subscriber/cursor ack/heartbeat/attendance/preparation/selected-runner evidence, Discord-thread attendance and preparation timeout preflight, fail-closed prepare/poll/grant behavior, verbose status and `stream.status` readiness diagnostics, and deterministic injected-clock tests. This remains control-local repository proof only: no live daemon activation, live Discord delivery, profile/provider/gateway/auth/token/model mutation, plugin/RUNFIX-012 implementation, production readiness, commit, push, or broad rollout is claimed. |

Plugin-owned `plugin/RUNFIX-012` now has local implementation proof under plugin KAH run `run-20260618T231811Z-ee5c6394d1fe` for explicit consumption of the new `control/RUNFIX-011` readiness evidence in activation planner/operator guardrails. `plugin/RUNFIX-013` now has local implementation proof under plugin KAH run `run-20260619T001719Z-9c41001040ef` for bundled/operator moderation hard rules that keep live councils lifecycle-first, prevent predeclared complete speaker-order debate simulation, require per-turn justified `speaker_selected` evidence, preserve `role_order` only as a per-turn justified option, and treat daemon `speech` as state authority. `plugin/RUNFIX-015` now has local implementation proof under plugin KAH run `run-20260619T071526Z-7d2ba33b07d5` for pure/local visible-author guard planning. Plugin-owned proof rows live in the plugin roadmap and shared RUNFIX SOT tables; this control roadmap records cross-repo traceability and does not convert plugin-owned proof into control implementation authority, live Discord delivery, production/live readiness, profile/provider/gateway/auth/token/model mutation, commit, push, or broad rollout readiness. `plugin/RUNFIX-017` now has plugin-owned local implementation proof under plugin KAH run `run-20260619T101255Z-189d01ba8b8f` for ARGUE quality-required prompt/harness hardening after `control/RUNFIX-016` local implementation proof; `plugin/RUNFIX-019` now has plugin-owned local implementation proof under plugin KAH run `run-20260619T214004Z-4d1e54b7304a` for daemon registry membership planner guidance after `control/RUNFIX-018` local implementation proof. This control roadmap records cross-repo traceability only and does not convert plugin-owned proof into control implementation authority.

| RUNFIX-014 | Selected-runner terminal accounting guard | local implementation proof | Control KAH run `run-20260619T051710Z-8e1f6efb61ec` adds selected-runner terminal accounting and report/status/export guardrails so `runner_invocation_started` + `runner_invocation_failed` are not hidden by later canonical `speech` events. Control summaries now expose runner started/succeeded/failed counts, canonical speech linkage, and fallback/manual harness flags before selected-runner or live-readiness claims. Trigger evidence: rename-council handoff artifact and matching failure session evidence with 15 runner failures and 15 speech events. Local verification passed focused storage/protocol tests, full `make test`, docs guardrails, plugin-contract checks, CodeGraph refresh gate, Red/Orange/Gray review, and KAH final gate. This does not claim live Discord delivery, production daemon activation, profile/gateway/provider/auth/token/model mutation, plugin/RUNFIX-015 runtime enforcement, push, broad rollout, or live readiness. |
| RUNFIX-016 | Summary schema robustness helper | local implementation proof | Control KAH run `run-20260619T083649Z-d10e1f5cc20b` final gate passed and local commit `9c15d22` adds `internal/storage/summary_turn_accounting.go` and export manifest key `summary_turn_accounting` so `channel.jsonl` and export bundles expose stable rows with turn, member, `speaker_selected_event_id`, `speech_event_id`, `runner_invocation_id`, and visible message id. The helper tolerates `payload.plugin_evidence` objects, explicit visible/Discord evidence objects/lists, missing optional evidence after `council_finalized`, and unsupported evidence maps/lists without crashing. Arbitrary unsupported maps/lists still fail closed: they do not become visible delivery proof, `selected_runner_pass`, live readiness, plugin readiness, or production readiness. Verification evidence exists in the KAH run for focused RUNFIX-016/RUNFIX-014/SURFD-002 storage tests, full `internal/storage`, `git diff --check`, `make test`, docs guardrails, plugin-contract check, and plugin `check-core-contract`; official color/final gate passed. |
| RUNFIX-018 | Explicit council roster registry reconciliation | local implementation proof | Control KAH run `run-20260619T214003Z-8a2afe33923f` adds daemon-owned registry reconciliation before `council.new`. Missing selected moderator/participant principals are persisted to `registry.yaml` only when the roster is explicit, the principal id is valid/non-reserved, and same-named wrapper resolution succeeds through the loaded allow-list. Disabled existing principals, invalid ids, unresolved wrappers, or ambiguous/missing identity evidence fail closed before session creation. The daemon reloads the registry, snapshots it into the session, and returns `registry_reconcile` evidence; subscription/heartbeat/ack readiness remains session-scoped. |


## RUNFIX2 — ATN discussion runtime usability hardening

RUNFIX2 is a five-task cross-repo epic created from the 2026-06-20 KLM/주유 live discussion dogfood. This control roadmap lists control-owned rows and bounded companion plugin status notes; plugin-owned implementation authority remains in `../../agent-turn-network-plugin/docs/roadmap.md`. The epic distinguishes production/operator enablement defaults from evidence-derived pass labels and does not authorize production activation, profile/provider/gateway/auth/token mutation, push, broad rollout, live pilot success, or unapproved live Discord delivery.

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| RUNFIX2-001 | Evidence/config semantics and terminal readiness model | completed/local-control | Local control implementation separates `generated_at` from `evaluated_at` / `evaluation_mode` / freshness-reference fields in `participant_runtime_readiness`. Open sessions still use current heartbeat/ack freshness; terminal councils evaluate readiness at the latest grant/turn evidence when present, or latest readiness evidence otherwise, so naturally stale post-final heartbeat/ack does not retroactively fail the original runtime prerequisites. `*_pass` labels remain evidence-derived and are not forced true. KAH run `run-20260620T083015Z-501b09d6d173` passed final gates with official Red/Orange/Gray review and Blue acceptance; push/live activation remain separately approval-bound. |
| RUNFIX2-002 | Selected-runner Hermes adapter response-generation fix | completed/local-control | Local control implementation changes the default Hermes selected-runner argv from delivery-only `send <prompt>` to response-generation `chat -Q -q <prompt>`, records `runner_invocation_succeeded` before linked canonical `speech`, defines compact JSONL with `payload.speech` as the selected-runner stdout producer contract, normalizes real Hermes CLI stdout by ignoring `session_id: ...` control lines, accepts single pretty/multiline JSON objects plus message/content/text-to-speech normalization as compatibility input, extends CLI `council.grant` timeout for selected-runner response generation, and keeps delivery/fallback output as terminal `adapter_command_mismatch` failure evidence that fallback/manual speech cannot repair. Verification now includes fake/isolated wrapper framing tests, a direct real-profile `jangbi chat -Q -q` stdout probe showing `session_id:` before JSON, and daemon/command tests for message-to-speech normalization plus grant timeout policy; no live Discord delivery, auth/token mutation, push, or production readiness claim. |
| RUNFIX2-003 | Discussion lifecycle closeout turns | completed/local-control | Local control implementation adds explicit `limits.max_discussion_turns` lifecycle accounting in status/export and blocks `council.propose` until T0 moderator opening, T1..Tmax selected participant discussion turns, and one selected closeout speech per participant are present. Total visible turns are `max_discussion_turns + participant_count + 2`. Existing `max_turns`, selected-runner accounting, and same-turn same-member `speaker_selected -> speech` validation remain separate. Local storage/daemon verification passed; plugin RUNFIX2-004 now has local rendering proof, and plugin RUNFIX2-005 now has local explicit-evidence planner/schema/test proof under plugin KAH run `run-20260620T131542Z-ae344b0d5ee9`. |
| RUNFIX2-005 | Integrated explicit discussion proof | local plugin implementation proof | Plugin-owned companion proof under KAH run `run-20260620T131542Z-ae344b0d5ee9` adds explicit-only `integrated_discussion_proof` support with separated lifecycle, selected-runner, participant runtime readiness at grant/turns, visible-surface, clean-transcript, visible-closeout, fallback, discussion-quality, and final-label axes. Selected-runner proof requires explicit runner success plus canonical linked speech for the selected member; manual/profile fallback remains diagnostic-only and cannot repair selected-runner failure. `live_readiness` remains false. This control roadmap records companion status only and does not claim live pilot success, production readiness, live Discord delivery, daemon/profile/provider/gateway/auth/token/model mutation, push, or broad rollout. |


## RUNFIX3 — Live-visible council contract hardening

RUNFIX3 is the 2026-06-25 follow-up from 주유's KLM live Discord thread postmortem and 마초 Blue triage. It preserves the RUNFIX lineage as an explicit epic-id exception like RUNFIX2 because the work directly hardens the same live-council readiness lane. The SOT is `17thHermes:40_outputs/team/macho/atn/2026-06-25-atn-live-visible-council-contract-hardening-sot.md`.

The failed run `sess_klm_selected_runner_20260625T085557Z` is diagnostic evidence only, not a successful live-visible council. RUNFIX3 hardens exact origin Discord thread binding, the `max_discussion_turns + participant_count + 2` visible-turn formula, selected-runner proof versus `moderator_direct`, delivery-evidence linkage, content/audit prompt separation, participant-to-participant dialogue mode, and drift repair/unresolved closeout. RUNFIX3 does not authorize production activation, live Discord rollout, daemon/profile/provider/gateway/auth/token/model mutation, push, package publication, or broad rollout.

Post-feedback plan amendment: RUNFIX3 freezes `selected_runner_pass` as an evidence-derived label rather than a daemon selection policy or `selection_mode`. `plugin/RUNFIX3-002` and `plugin/RUNFIX3-003` consume this frozen control evidence contract. Final RUNFIX3 completion requires a named closeout artifact, negative fail-closed matrix evidence, Red/Orange/Gray review, 마초 Blue synthesis, and 주군 approval.

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| RUNFIX3-001 | Live-visible council contract SOT and roadmap lock | completed | Docs-only cross-repo SOT lock in `17thHermes:40_outputs` records the failed 2026-06-25 KLM thread run as diagnostic-only evidence, fixes the expected lifecycle as T0 moderator opening, T1..TN selected participant discussion, TN+1..TN+P participant closeouts, and TN+P+1 moderator synthesis, and defines the four-PR sequence for plugin guidance, plugin planner/schema evidence, and control diagnostics. Review evidence is recorded in the SOT color review log. |
| RUNFIX3-002 | Moderator/operator live-thread contract hardening | implementation_complete/review_pending | Plugin-owned guidance hardening is implemented with bounded operator/moderator rule ownership, fixed minimum plugin gates passed, and cross-repo mirrors synchronized. Review closeout is still pending focused acceptance; planner/schema/runtime fields remain deferred to `plugin/RUNFIX3-003`, and control diagnostics/enforcement remain deferred to `control/RUNFIX3-004`. |
| RUNFIX3-003 | Planner and evidence schema for live-thread proof axes | implementation_complete/review_pending | Plugin-owned planner/schema/operator evidence against the frozen control evidence contract is implemented and awaiting focused reviewer acceptance. Activation/final reports now carry exact-origin fail-closed proof, formula-grounded visible-turn accounting, start-gated daemon registry membership, and separate RUNFIX3 closeout/synthesis/delivery-target/prompt-envelope/dialogue/drift/fail-closed report axes without promoting live readiness. Implementation and fixed minimum test gates passed: `git diff --check`, `make docs-guardrails`, `make check-core-contract`, `make test-prepare`, completed `make test-unit` as a deliberate tightening over the older waiver wording, focused `uv run python -m pytest tests/unit/test_discussion_activation_planner.py tests/unit/test_tool_schemas.py tests/unit/test_tool_handlers.py tests/unit/test_core_contract.py tests/unit/test_diagnostics_decoding.py tests/unit/test_surface_rendering.py`, and positive/negative sample planner or final-report payload. Pending review/acceptance gates: focused Red/Orange/Gray review and Blue synthesis. |
| RUNFIX3-004 | Control live-council diagnostics and enforcement follow-up | implementation_complete/review_pending | Control diagnostics/enforcement implementation is in review against the frozen evidence-label policy: `selected_runner_pass` remains durable evidence, not daemon `selection_mode` or policy. The current implementation connects lifecycle accounting, selected-runner/delivery-evidence requirements, exact thread binding diagnostics, missing-stage preflight/status/finalization diagnostics, and fail-closed propose/finalize behavior with unresolved as the explicit terminal alternative. Fixed minimum implementation gates have passed; focused Red/Orange/Gray review and Blue synthesis remain pending acceptance. Durable verification evidence: `docs/evidence/2026-06-26-runfix3-004-diagnostics-evidence.md`. |

## NEXFIX — Selected-runner prompt envelope remediation

NEXFIX is the 2026-06-26 follow-up from 주유's selected-runner prompt envelope defect report: `17thHermes:40_outputs/team/jooyoo/atn/2026-06-26-atn-selected-runner-prompt-envelope-defect-report.md`. The original prompt-envelope docs-lock SOT is `17thHermes:40_outputs/team/macho/atn/2026-06-26-atn-selected-runner-prompt-envelope-nexfix-sot.md`. The live-visible runtime delivery follow-up SOT is `17thHermes:40_outputs/team/macho/atn/2026-06-28-atn-live-visible-selected-runner-runtime-delivery-newfix003-sot.md`. `NEXFIX` is an explicit naming exception: 주군 requested the epic label `NEXFIX` and task ids using the `NEWFIX-*` prefix.

The first observed defect was content-plane readiness failure after lifecycle/runtime readiness passed: `agenda_locked` contained the KLM decision question and success criteria, but the default selected-runner prompt envelope omitted agenda/prior-context, so the first selected participant said the agenda body was missing. The 2026-06-28 follow-up defect is live-visible selected-runner runtime delivery instability: the repaired Discord-thread session had `channel_id + thread_id`, posted some selected-runner turns, but failed to finish because selected-runner runner generation and Discord visible delivery shared a too-small effective dispatch budget and selected-member prompt continuity only used a bounded recent context window. NEXFIX does not authorize production activation, live Discord rollout, profile/provider/gateway/auth/token/model mutation, push, package publication, or broad rollout.

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| NEWFIX-001 | Projection-backed selected-runner prompt envelope | completed | Control-owned prompt-envelope hardening is accepted after focused color review and traceability repair. The daemon builds selected-runner prompts from authoritative projection, records durable `selected_runner_prompt_evidence`, fails closed on missing required context, and rejects nil/empty selected-speaker prompt builders before runner launch. Verification passed: `git diff --check`, `go test ./internal/storage ./internal/daemon -run 'NEWFIX001|SelectedRunnerPrompt|SelectedSpeaker|RUNFIX009|RUNFIX011' -count=1`, and `make test`. No live-readiness claim is introduced. |
| NEWFIX-002 | Plugin content-plane readiness guidance mirror | completed | Plugin-owned moderator/participant guidance hardening is accepted after focused color review and traceability repair. Control records this cross-repo mirror/status row only; plugin remains the owner for packaged guidance, and control does not treat plugin guidance as daemon prompt authority. Verification passed in plugin: `git diff --check` and `make test`. |
| NEWFIX-003 | Live-visible runtime delivery SOT and diagnosis lock | completed | Cross-repo docs-only SOT lock records Jooyoo's 2026-06-28 KLM live-visible delivery blocker, corrects the effective selected-speaker timeout diagnosis to 30 seconds when no session `dispatch_timeout_sec` is set, separates timeout/runtime delivery from prompt-continuity defects, and freezes the implementation-ready follow-up split for `control/NEWFIX-004`, `control/NEWFIX-005`, and `plugin/NEWFIX-006`. This row is a SOT/roadmap lock only and does not claim live readiness or selected-runner success for the failed session. |
| NEWFIX-004 | Selected-runner own-history prompt context | implementation_complete/review_pending | Control-owned selected-runner own-history prompt context is implemented and awaiting focused reviewer acceptance. Current scope keeps recent whole-council context with the reviewed window of 6, adds selected-member own-history context (all prior speeches, latest valid claims, and fail-closed own claim-index reconstruction), extends `selected_runner_prompt_evidence` with distinguishable global and own-history source ids, and adds round-robin continuity regression coverage. Fixed minimum control gates passed; focused re-review remains pending. |
| NEWFIX-005 | Live-visible selected-runner timeout policy | implementation_complete/review_pending | Control-owned follow-up is implemented for Discord live-visible selected-runner councils: it requires `dispatch_timeout_sec=120` or an approved explicit alternative, projects typed `selected_runner_timeout_evidence` through council status and `bundle_manifest.json`, blocks guarded timeout drift, leaves `stream.status` unchanged, and has passed focused daemon/storage verification. |
| NEWFIX-006 | Plugin live-visible planner/operator guardrail | implementation_complete/review_pending | Plugin-owned follow-up is implemented with a dedicated activation-planner/operator guardrail that consumes the `NEWFIX-004` prompt own-history evidence and `NEWFIX-005` timeout evidence, requires visible-surface readiness plus daemon registry membership before Discord-origin live-visible start-capable status, tightens approved-alternative timeout validation, and keeps local-only/artifact/manual bridge paths labeled as non-equivalent fallback. Focused plugin re-review remains pending. |
| NEWFIX-007 | Agenda lock required-context submission contract | completed | Control-owned follow-up from Jooyoo's cancelled-session diagnosis. The implementation repairs `atn-control council lock-agenda` structured agenda submission so `decision_question`, `success_criteria`, and `out_of_scope_policy` are required at the CLI and daemon/storage event boundary, aligns CLI/spec/conformance fixtures with selected-runner mandatory agenda context, and makes agenda `--from-file` JSON behavior explicit/fail-closed instead of silently storing a `draft` string. Request-feedback and handle-feedback gates are accepted after focused Red/Orange/Gray re-review plus Blue synthesis; MAR completed after the prior architecture status-surface coherence finding was remediated, focused arch re-review passed, and Blue disposition allowed KAH `mar_review` completion; second-color review has also passed and final verification and improve/lessons are complete. Second-color review, final verification, and improve/lessons gates are complete. This is not a live-readiness, Discord rollout, commit, or push claim. |
| NEWFIX-008 | Plugin council command representation/idempotency contract | completed | Plugin-owned follow-up implemented the live Unix socket `command.submit` representation so structured `council.lock_agenda` agenda fields remain under daemon-owned `params.payload`, daemon `payload.command_id` authority is explicit, and unrepresentable caller `idempotency_key` values fail closed before socket connect. Control records this cross-repo mirror/status row only; prior MAR arch mirror/status-surface remediation passed focused re-review and Blue MAR disposition accepted KAH `mar_review` completion; second-color review has also passed and final verification and improve/lessons are complete. Second-color review, final verification, and improve/lessons are complete. |
| NEWFIX-009 | Live-visible selected-runner agenda-context acceptance proof | completed | Cross-repo acceptance proof depending on `control/NEWFIX-007` and `plugin/NEWFIX-008` now has focused local evidence plus post-feedback remediation: control daemon/storage/CLI tests verify `decision_question`, `success_criteria`, and `out_of_scope_policy` in `agenda_locked.payload`, `selected_runner_prompt_evidence.included_context`, and agenda source refs, while missing, empty, and whitespace-only required fields fail closed at storage/CLI submission and no-agenda selected-runner dispatch records blocked prompt evidence plus `selected_runner_dispatch_failed`. Plugin focused live-transport tests verify structured `council.lock_agenda` payload preservation under daemon-owned `params.payload`. Red approved initial review; Orange/Gray request-changes were remediated; focused Orange/Gray re-review and Blue synthesis approved MAR entry; all five MAR roles are covered and Blue MAR disposition allowed KAH `mar_review` completion. The cancelled `sess_council_1782671073534714000` remains defect evidence, not success evidence; post-MAR second-color review, KAH final verification, and improve/lessons are complete; live rollout, broad readiness, commit, and push are not claimed. |

## COUNCIL-STAB — Live-visible selected-runner stabilization

The 2026-06-30 Jooyoo KLM token/speed council blocker is a release blocker for any renewed live-visible selected-runner council claim until the same cross-repo task proves the full path: stance-bearing `hand_raise`, derived grant `stance_assignment`, selected runner invocation, linked canonical speech, visible delivery evidence, and terminal closeout/status coherence. The failed historical session `sess_klm_token_speed_20260629T235032Z` remains diagnostic evidence only and must not be counted as acceptance.

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| COUNCIL-STAB-001 | Live-visible selected-runner golden path | completed/local-proof | Cross-repo stabilization implementation is complete through the KAH improve/lessons gate for local deterministic proof: control fails closed when a selected-runner grant lacks a matching hand_raise `intent`/`reason`, supports `council grant --member` as a `--to` alias, preserves terminal append metadata/status after post-terminal diagnostics, and adds a 15-turn live-visible selected-runner regression with terminal closeout/status evidence included in `make test-regression` and `make test`; plugin rejects `council.hand_raise` without `intent` or `reason` before transport submit and updates schema/operator guidance. Red and Orange approved the bounded evidence; the prior Gray status/SOT traceability blocker was remediated and Gray post-MAR review approved with findings; Blue second-color synthesis allowed KAH to proceed to `final_verify`; final verification gates passed; improve/lessons were recorded. Evidence is local deterministic proof only; no live Discord rerun, production activation, profile/provider/gateway/auth/token mutation, push, or broad readiness is claimed. |

## LVCOR — Live-visible council one-pass reliability

LVCOR is the five-task cross-repo request-changes epic created from Jooyoo's 2026-06-30 one-pass live-visible council reliability report: `17thHermes:40_outputs/team/jooyoo/atn/2026-06-30-atn-developer-request-changes.md`. The epic fixes the standard operator path so live-visible council completion is parameterized by `max_discussion_turns = n` and `participant_count = p`: T0 moderator opening, T1..Tn selected participant discussion, T(n+1)..T(n+p) participant closeouts, and T(n+p+1) moderator synthesis / terminal closeout. Expected visible turns are `n + p + 2`; hard-coded T20 handling is invalid except for the specific 15-turn/4-participant instance.

LVCOR did not by itself authorize production activation, live Discord rollout, default Discord activation, daemon/profile/provider/gateway/auth/token/model mutation, package publication, push, or broad rollout. The later default-setting update promotes Discord-origin decision-bearing councils to the standard live-visible lifecycle using the completed LVCOR/NEWFIX proof stack and Jooyoo live-success evidence while keeping production readiness, push, and runtime/profile/provider/gateway/auth/token mutation separately approval-gated.

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| LVCOR-001 | Dynamic lifecycle accounting guard | completed/control-local | Control-owned status/guard implementation for parameterized live-visible councils. KAH run `run-20260701T051848Z-90ca0c0febba` adds derived expected visible turns, discussion-turn completion, participant-closeout completion, moderator opening/synthesis presence, terminal-visible proof status/blocker, terminal phase, and completion verdict. Finalized reporting fails closed unless terminal visible closeout proof is `posted`; failed, pending, unproven, missing, malformed, or mismatched proof is not success-like completion. GJC/KAT/Go gates passed; official color review, MAR, post-MAR second color review, and Blue final synthesis accepted with non-blocking carry-forwards. This row does not claim live Discord delivery, runtime activation, production readiness, profile/provider/gateway/auth/token/model mutation, commit, push, or broad rollout. |
| LVCOR-002 | Parameterized terminal synthesis and finalize proof | completed/control-local | Control-owned terminal moderator synthesis/finalize path implemented in KAH run `run-20260701T093808Z-bfd2968703e2`. Status now exposes terminal synthesis turn/index/event/summary fields derived from `max_discussion_turns` and participant count, not a hard-coded T20. Standard live-visible `discord_thread` finalization fails closed unless posted terminal visible closeout proof has explicit `surface_evidence.status=posted`, the configured thread binding, and a visible-message pointer (`final_message_id`, `message_id`, `message_ref`, or explicitly typed equivalent). `kanban_comment_id`, `vault_decision_note`, and generic return evidence are linked-authority evidence only, not visible-surface proof; malformed, failed, pending, unproven, missing-pointer, missing-thread, and wrong-thread evidence are not finalized success. CLI `council finalize --from-file` and `council unresolved --from-file` use command-specific structured JSON with fail-closed schema checks. `council.unresolved` remains the honest terminal alternative. Red/Gray REQUEST_CHANGES remediation and focused re-review were accepted; MAR recovery rerun covered logic, security, arch, cve, and test_adequacy with merge-pack PASS and Blue PASS_WITH_FINDINGS disposition. Official post-MAR Red/Orange/Gray review plus dependent Blue synthesis accepted the second-color gate, KAH `second-color-review` completed at workflow revision 37, and final KAH closeout completed at `evt-003521`, revision 39, with `final-report.md` plus `gate-reports/final.json`. Commit, push, live Discord delivery, runtime activation, production readiness, profile/provider/gateway/auth/token/model mutation, and broad rollout remain unclaimed. |
| LVCOR-003 | CLI schema alignment and timeout semantics | completed/control-local | Control-owned CLI/daemon operator UX repair is implemented, verified, feedback-handled, MAR-reviewed, post-MAR color-reviewed, and final-closed in bounded KAH run `run-20260702T100541Z-519854f32aea`; post-MAR official second-color review completed at KAH event `evt-003600`, workflow revision 37, and final KAH closeout completed at `evt-003602`, revision 39, with `.kkachi/runs/run-20260702T100541Z-519854f32aea/final-report.md` plus `gate-reports/final.json`. Current artifacts add canonical `--turn` help/discoverability while keeping `--round` as a CLI compatibility alias, structured lifecycle `--from-file` validation that rejects legacy `payload.round` with a `turn` migration hint, and `council.grant` status reporting that distinguishes durable append acceptance from selected-runner timeout/failure follow-up through `append_status`, `dispatch_status`, `runner_status`, `speech_link_status`, and event pointers. This row does not claim live Discord delivery, runtime activation, production readiness, profile/provider/gateway/auth/token/model mutation, commit, push, or broad rollout. |
| LVCOR-004 | Plugin lifecycle envelope golden tests | completed/plugin-local | Plugin-owned typed tool reconciliation for the full live-visible lifecycle is accepted for bounded local proof in plugin run `run-20260702T141926Z-a716fc43a7d2` and commit `1137c92`. Control records this as a dependency/mirror row only: plugin preserves caller-supplied IDs, requires canonical `payload.turn` for turn-bearing council commands, rejects legacy nested `payload.payload.round`, and does not own daemon lifecycle/log/cursor/dedupe state. This row does not claim live Discord delivery, runtime activation, production readiness, profile/provider/gateway/auth/token/model mutation, push, release, or broad rollout. |
| LVCOR-005 | Parameterized full-shape acceptance proof | completed/plugin-local-final | Plugin-owned local implementation and fixed verification evidence exist in plugin run `run-20260702T195344Z-341cf06c02d6`: the proof surface consumes completed LVCOR-001 through LVCOR-004 rows and proves both 15-turn/4-participant -> 21 visible turns and 5-turn/2-participant -> 9 visible turns, zero runnerless/manual selected turns, participant closeouts, T(n+p+1) terminal moderator synthesis, and finalized-vs-explicit-unresolved label separation. Focused status-remediation re-review accepted the original stale-row blocker, strict MAR rerun passed with all required roles covered, KAH `mar-review` completed at `evt-003295`, and post-MAR second-color review completed at `evt-003302` with Blue `APPROVE_WITH_FINDINGS / ATN_BLUE_SECOND_COLOR_ACCEPTED_WITH_FINAL_STATUS_RECONCILIATION_REQUIRED`; final KAH closeout completed at `evt-003309`, workflow revision 41, with `final-report.md` and `gate-reports/final.json`. This control row is a mirror/status note only and does not claim live Discord delivery, runtime activation, production readiness, profile/provider/gateway/auth/token/model mutation, commit, push, release, or broad rollout. |

## PRSLR — Persistent runtime session latency repair

PRSLR is the twelve-task cross-repo implementation epic for the persistent participant runtime SOT and the follow-on dialogue-quality hardening planned from the 2026-07-05 KLM token/speed final report (`17thHermes:40_outputs/team/jooyoo/atn/klm-token-speed-20260705T174553Z/final_report.md`). PRSLR-001 through PRSLR-007 repair the latency/cost bug where participant turns can open fresh sessions and re-inject full or partial history instead of maintaining council-scoped participant actors. PRSLR-008 through PRSLR-011 harden the resulting runtime so selected-runner success, canonical speech linkage, prior-speaker ARGUE relation evidence, plugin guidance, and E2E discussion-quality proof are all separately evidenced before the epic is finally closed. PRSLR-012 is the direct-control follow-up requested by 주군 after the 2026-07-07 live KLM audit: visible Discord delivery echoes must be classified as delivery evidence, not second canonical speeches or fallback repairs.

PRSLR is a default-off/local-only implementation lane. It does not authorize live Discord rollout, production activation, daemon/profile/provider/gateway/auth/token/model mutation, push, package publication, or broad readiness. PRSLR-001 has locked the plan/evidence matrix through KAH/KAS plan-vet, explicit ask authorization, docs/evidence contract-lock implementation evidence, official implementation review, MAR, post-MAR second-color review, final verification, and local improvement/status closeout. Participant sessions are council-scoped only; cross-council participant session reuse is prohibited. Rehydrate/observe/ack failures default to whole participant runtime/council blocked, and affected-member degraded continuation requires later explicit approval.

Control-owned rows are `control/PRSLR-002` through `control/PRSLR-005`, then `control/PRSLR-008`, `control/PRSLR-009`, and `control/PRSLR-012`. `plugin/PRSLR-006` and `plugin/PRSLR-010` are listed here as dependency/mirror rows only; plugin status authority remains `../../agent-turn-network-plugin/docs/roadmap.md`. Cross-repo `PRSLR-001`, `PRSLR-007`, and `PRSLR-011` require both roadmap/status surfaces to stay synchronized.

| Task ID | Task Title | Task Status | Task Description |
| --- | --- | --- | --- |
| PRSLR-001 | Plan-vet and implementation contract lock | completed/local-proof | Cross-repo KAH/KAS plan-vet task for the persistent participant runtime SOT. KAH run `run-20260704T064925Z-d66241afb882` completed classification, CodeGraph refresh, KAH-wrapped GJC RALPLAN, official Red/Orange/Gray plan-vet, Blue synthesis, explicit ask approval, KAH-wrapped GJC ULTRAGOAL candidate evidence, local implement/enhance-test/optimize nodes for the docs/evidence contract lock, official implementation review/request-feedback handling, MAR provider review with Blue disposition, post-MAR second-color review, final verification, and local improvement/status closeout. This row does not claim PRSLR-002..007 runtime implementation, live activation, production readiness, daemon/profile/provider/gateway/auth/token/model mutation, commit, push, package publication, or broad readiness. |
| PRSLR-002 | Drop protocol and command validation | completed/local-proof-committed | Control-owned protocol/storage slice implemented under KAH run `run-20260704T093816Z-a01b9bbc6b36`: first-class `council.drop` routes to canonical `hand_raise_dropped`, legacy `council.hand_raise` with `wants_to_speak=false` is rejected instead of translated, drop validates member actor, discussion phase, turn, reason, in-session matching `request_event_id`, idempotent same-command retry, and conflicting duplicate responses, and projection/status/conformance fixtures preserve drop fields. Official request_feedback found a Red blocker for forged/manual `auto=true`; feedback handling now rejects `auto=true` `council.drop` at storage/API and CLI surfaces, updates help discoverability, and records focused RED/GREEN remediation evidence. Local evidence includes targeted RED/GREEN storage tests, CLI/daemon route tests, `make test`, KAT status attachment, and GJC ULTRAGOAL evidence; focused post-feedback review, MAR rerun-1, and post-MAR second-color review are complete. Generation freshness, observed_cursor coverage, latest-public-event checks, daemon-owned auto-drop windows, and participant-session cursor authority remain deferred to PRSLR-003/004 and PRSLR-007 final integration acceptance. This row does not claim live activation, production readiness, push, package publication, or cross-repo PRSLR completion. MAR rerun-1 PASS covered logic/security/arch/cve/test_adequacy with no unresolved required roles; Blue disposition accepted low findings, remediated forged/stale request_event_id standalone tests, and Blue synthesis accepted post-MAR second-color review at KAH event `evt-003717`, workflow revision 23; final_verify and improve closeout passed. |
| PRSLR-003 | Response window scheduler and replay | completed/local-proof | Control KAH run `run-20260704T131454Z-90e282c0e034` completed the bounded local PRSLR-003 response-window scheduler/replay slice through classification, CodeGraph refresh, KAH-wrapped GJC RALPLAN, official plan-vet Blue acceptance, explicit ask approval, GJC ULTRAGOAL, implementation/enhance-test/optimize/docs gates, implementation review REQUEST_CHANGES remediation, focused re-review, MAR provider review with post-MAR remediation, official post-MAR second-color review, and final verification (`evt-003768`). The implementation proves local/default-off 120s response-window opening on `council.poll`, all-members-responded early close, late `council.hand_raise` and late manual `council.drop` fail-closed without append, daemon-owned timeout auto-drop with deterministic replay dedupe, and idempotent retry preservation. Commit, push, live/runtime activation, production readiness, Discord delivery, provider/profile/gateway/auth/token/model mutation, plugin runtime readiness, and PRSLR-004+ readiness are not claimed. |
| PRSLR-004 | Participant session registry and all-member observe coverage | completed/local-proof-commit-ready | Control-owned persistent per-council per-member participant session registry with handle/generation/last cursor/member lock evidence, public speech observe delta fan-out to non-speakers, speaker authored canonical commit/self-ack, selected-member same-session speech resume, and all-member cursor coverage proof. KAH run `run-20260704T172825Z-4af3d101effe` completed classification, CodeGraph refresh, KAH-wrapped GJC RALPLAN planning, local plan gate, official Red/Orange/Gray plan-vet, and Blue synthesis `ATN_BLUE_PRSLR004_PLAN_VET_ACCEPTED_WITH_CARRY_FORWARDS`; explicit ask approval authorized bounded local-control implementation, bounded local-control implementation reached official implementation review; Red/Orange passed, the accepted Gray/Blue traceability blocker was remediated, focused Gray re-review passed, and Blue focused synthesis `ATN_BLUE_PRSLR004_FOCUSED_REREVIEW_ACCEPTED` accepted `handle_feedback` for the separate MAR gate. MAR provider review passed after recovery rerun-1 and Blue disposition `ATN_BLUE_PRSLR004_MAR_ACCEPTED_WITH_LOW_FINDINGS` accepted low/info carry-forwards for the separate post-MAR second-color gate; post-MAR second-color review then passed after focused Gray traceability re-review and Blue synthesis, and KAH `final_verify` and `improve` closeout passed. Required carry-forwards are stale generation/cursor/request_event_id negative tests, bounded PRSLR-004 scope without PRSLR-005/006/007 claims, concrete `persistent_participant_runtime_evidence` status/export rows, and GJC RALPLAN receipt-only treatment. No live/runtime activation, production readiness, plugin runtime implementation, commit, push, or broad rollout is claimed. |
| PRSLR-005 | Delta prompt policy and rehydrate fail-close | completed/local-proof | Control KAH run `run-20260704T201529Z-d74b66d339da` completed classification, CodeGraph refresh, KAH-wrapped GJC RALPLAN receipt evidence, local plan gate, official Red/Orange/Gray plan-vet, Blue synthesis `ATN_BLUE_PRSLR005_PLAN_VET_ACCEPTED_WITH_CARRY_FORWARDS` / `ACCEPT_PLAN_FOR_KAH_ASK`, explicit 주군 ask approval, KAH-wrapped GJC ULTRAGOAL setup evidence, local implement/enhance-test/optimize/update-docs gates, official implementation review, handle-feedback remediation, MAR provider review, Blue MAR disposition, and post-MAR second-color review. The local implementation adds `participant_runtime_mode=persistent_delta`, `hot_prompt_strategy=delta_only`, explicit `stateless_fallback=false`, full-history/own-history hot-lane rejection markers, delta vs rehydrate source refs, rehydrate identity/source validation status/failures, and whole participant runtime/council blocked evidence via `runtime_block_status=blocked_rehydrate_failed` on validation failure. Handle-feedback remediated stale/missing cursor/source blocks and clean transcript PRSLR-005 field rendering; focused remediation tests, full `make test-prepare`, `make test`, `make docs-guardrails`, plugin `make docs-guardrails`, and control/plugin `git diff --check` passed locally. Focused Red re-review `t_1687ba56` accepted the cursor/source fail-closed remediation; Blue disposition `ATN_BLUE_PRSLR005_MAR_ACCEPTED_WITH_LOW_INFO_CARRYFORWARDS` accepted MAR provider PASS with explicit low/info carry-forwards; KAH `mar_review` completed at `evt-003851`; post-MAR Red/Orange/Gray review cards returned `PASS_WITH_FINDINGS`; Blue second-color synthesis `ATN_BLUE_PRSLR005_SECOND_COLOR_ACCEPTED_WITH_CARRYFORWARDS` completed KAH `second_color_review` at `evt-003855`; KAH `final_verify` completed at `evt-003857`; KAH `improve` completed at `evt-003859`, workflow revision 27, with no ready nodes remaining. Accepted carry-forwards remain attached to PRSLR-007/future claim-expansion gates. Commit, push, live/runtime activation, production readiness, plugin runtime implementation, and broad rollout are not claimed. |
| PRSLR-006 | Plugin command and evidence-surface alignment | plugin-completed-local-proof-commit-ready dependency | Plugin-owned typed-tool/schema/operator slice under plugin KAH run `run-20260705T094456Z-e71c7b7d9795`: implementation/review/feedback handling is complete; initial MAR `DEGRADED` was held and Red-adjudicated; strict MAR rerun passed with all required roles covered; KAH `mar_review` completed at plugin `evt-003350` after run-local state repair; official post-MAR second-color review completed at plugin `evt-003354` with Red `t_84bd14ba`, Orange `t_c01cc684`, Gray `t_12e94e1a`, and Blue synthesis `t_a78ee68b` accepting final_verify routing with carry-forwards; plugin KAH `final_verify` completed at `evt-003356`; plugin KAH `improve` completed at `evt-003358`, workflow revision 29. Control records this as dependency/mirror status only and does not claim plugin runtime activation, commit/push, live readiness, provider/profile/gateway/auth/token/model mutation, or PRSLR-007 cross-repo acceptance. |
| PRSLR-007 | Cross-repo persistent-runtime proof and documentation closeout | completed/local-proof-commit-ready | Cross-repo persistent-runtime proof task for the original PRSLR-001..007 scope under primary control KAH run `run-20260705T132654Z-ae2091713390`: classification, CodeGraph refresh, KAH-wrapped GJC RALPLAN, plan gate, official read-only plan-vet, explicit 주군 ask approval, KAH-wrapped GJC ULTRAGOAL setup, implementation/proof packaging, enhance-test, optimize/slop-cleanup, docs/status update, request-feedback/handle-feedback, MAR provider review, Blue MAR disposition, post-MAR second-color, final verification, and improve closeout are complete. Local/default-off proof artifacts are under `artifacts/acceptance/`: `prslr007-acceptance-matrix.json`, `persistent-participant-runtime-evidence.json`, `plugin-consumption-proof.json`, `operator-report.md`, and `manifest.json`; full verification evidence is recorded in `artifacts/verification/full-verification-prslr007.log`. The local proof package covers session reuse, prompt-context reduction, all-member cursor coverage, response-window timing/accounting, rehydrate/fail-close status, plugin consumption boundary, and explicit non-claims. Commit, push, live/runtime activation, production readiness, provider/profile/gateway/auth/token/model mutation, Discord delivery, and post-PRSLR activation remain unclaimed and approval/review-gated. |
| PRSLR-008 | Selected-runner invocation repair and canonical speech linkage | completed/local-proof-committed | Control-owned repair for the post-PRSLR KLM audit finding where selected-runner invocation failure and fallback/manual profile text could mask missing `selected_runner_pass`. KAH run `run-20260705T191135Z-83a7a11db6cf` completed classification, CodeGraph refresh, KAH-wrapped GJC RALPLAN receipt evidence, local plan gate, official Red/Orange/Gray plan-vet, Blue synthesis, explicit 주군 ask approval, KAH-wrapped GJC ULTRAGOAL setup evidence, and local implement/enhance-test/optimize/update-docs nodes. Official implementation review returned Red `REQUEST_CHANGES` for missing selected-turn fail-open, with Orange/Gray `APPROVE_WITH_FINDINGS`; Blue accepted the blocker at `feedback-1.md` and KAH `request_feedback` completed at `evt-003934`. The handle-feedback remediation now requires linked runner speech to match the same selected member, positive selected turn, runner invocation, durable `runner_invocation_succeeded`, and selected-event causation before `selected_runner_pass`; fallback/manual/runnerless, wrong-turn, or missing-selected-turn speech remains diagnostic-only. Transcript summaries include a Selected Runner Evidence Table with per-grant event ids/status. Local verification includes RED/GREEN wrong-turn, missing-selected-turn, and transcript-table tests, focused RUNFIX014/RUNFIX3004/PRSLR008 selected-runner tests, KAT `test-prepare`, KAT `go-test`, `make test`, docs guardrails, and a stability rerun. Focused Red and Orange re-reviews accepted the handle-feedback remediation. MAR recovery rerun-1 passed with all required roles covered, Blue MAR disposition accepted the recovery packet, and KAH `mar_review` completed at `evt-003949`. Post-MAR second-color review originally held on Orange/Gray status-packet blockers; remediation refreshed checklist/roadmap/live-transport status surfaces, focused Orange `t_7daa51ed` returned `APPROVE_WITH_FINDINGS / ATN_ORANGE_PASS_WITH_UX_RISK`, focused Gray `t_e6d6decb` returned `APPROVE_WITH_FINDINGS / ATN_GRAY_RECORDED`, and Blue focused synthesis accepted remediation in `.kkachi/runs/run-20260705T191135Z-83a7a11db6cf/second-color-focused-rereview-blue-synthesis.md`. KAH `second_color_review` completed at `evt-003956`, workflow revision `23`; KAH `final_verify` completed at `evt-003958` after fresh control/plugin verification, local improve/status closeout is recorded in `improvement-note.md`, and the approved local commit records this bounded closeout. Push, live/runtime activation, Discord delivery, production readiness, provider/profile/gateway/auth/token/model mutation, package publication, broad rollout, and cross-repo PRSLR epic completion remain unclaimed and approval-gated. |
| PRSLR-009 | Prior-speaker ARGUE quality contract and daemon diagnostics | completed/local-proof-committed | Control-owned quality-contract slice for the requirement that every non-opening participant speech must engage at least one previous speaker through structured ARGUE relation evidence. KAH run `run-20260706T092628Z-8b113d0833db` completed classification, CodeGraph refresh, KAH-wrapped GJC RALPLAN receipt evidence, official Red/Orange/Gray plan-vet plus Blue synthesis, explicit 주군 ask approval, KAH-wrapped GJC ULTRAGOAL setup, implementation, enhance-test, optimize, docs update, official implementation review, no-source-change handle-feedback, MAR provider/script review, post-MAR second-color review, final verification, and improve closeout. MAR merge-pack status is `PASS` with all required roles covered and `unresolved_required_roles=[]`; KAH `mar_review` completed at `evt-003995`. Initial post-MAR Orange/Gray status-surface blockers were remediated through focused Orange `t_f02d2a61`, focused Gray `t_8d701d8e`, focused Blue `t_0a5a0d9d`, and final second-color Blue synthesis `APPROVE_WITH_FINDINGS / ATN_BLUE_PRSLR009_SECOND_COLOR_ACCEPTED_WITH_FOCUSED_REMEDIATION`; KAH `second_color_review` completed at `evt-004002`. Final verification passed local control/plugin checks and completed at `evt-004007`; KAH `improve` completed at `evt-004009`, workflow revision `27`. Current local implementation evidence adds prior-speech-count opening-window handling, per-speech and status-level `prior_speaker_argue_quality_evidence`, fail-closed quality-required rejection for first non-opening orphan speech even when `responds_to_event_id` exists, valid prior speech/claim target accounting, explicit justified `new_axis` exception accounting, and projection preservation through relation audit JSON. Focused PRSLR-009/RUNFIX-009 storage and daemon tests, full `go test ./...`, KAT `go-test`/`test-prepare`, docs/contract checks, control/plugin stale lifecycle scans, and final diff manifest pass locally. Commit, push, live/runtime activation, production readiness, provider/profile/gateway/auth/token/model mutation, package publication, broad rollout, and PRSLR epic completion remain unclaimed and separately approval-gated. |
| PRSLR-010 | Plugin prior-speaker prompt/schema/operator hardening | plugin-completed/local-proof-committed dependency | Plugin-owned mirror/consumer row: update `atn_selected_participant_response`, `atn_council_command` operator guidance, bundled `atn-participant`/`atn-moderator` skills, schemas, and local validation fixtures so non-opening responses include at least one valid prior-speaker `stance_links[]` entry before canonical speech submission when sufficient prior context exists. Plugin KAH run `run-20260706T120949Z-6faa592a3ca8` completed task classification as `development` with KAT `test-prepare` PASS, KAS route-backed `development_full` workflow materialization, CodeGraph refresh at `evt-003370`, KAH-wrapped GJC RALPLAN receipt at `evt-003371`, KAH `plan` at `evt-003373`, official Red/Orange/Gray plan-vet, Blue synthesis `t_02af4d06`, explicit ask approval, KAH-wrapped GJC ULTRAGOAL setup, local implementation, and enhance-test verification. The plugin local proof hardens schema/guidance/tests so non-opening `quality_required` selected responses with sufficient caller context require caller-target-valid `participant_response.stance_links[]` or a justified `new_axis` exception, while display/prose hints remain non-authority. Control records this as dependency/mirror status only; plugin must not invent daemon lifecycle, cursor, response-window, dedupe, selected-runner, or discussion-quality pass state. Official implementation review, feedback handling, MAR strict rerun, and Blue MAR disposition are complete; plugin KAH `second_color_review` completed at `evt-003397`, `final_verify` completed at `evt-003399`, and local improve/status closeout has prepared commit-ready handoff. Local commits `ce988e2` (plugin) and `fc673b4` (control mirror) now record PRSLR-010; push, live/runtime activation, production readiness, provider/profile/gateway/auth/token/model mutation, package publication, broad rollout, and PRSLR-011 acceptance remain unclaimed. |
| PRSLR-011 | Cross-repo dialogue-quality E2E acceptance proof and docs closeout | completed/local-proof-committed | Cross-repo final proof after PRSLR-008 through PRSLR-010. Primary control KAH run `run-20260706T144956Z-c4f49751f7f4` completed classification, CodeGraph refresh `evt-004020`, KAH-wrapped GJC RALPLAN receipt, KAH `plan` `evt-004024`, official Red/Orange/Gray plan-vet plus focused Gray remediation, Blue synthesis `ATN_BLUE_PRSLR011_PLAN_VET_ACCEPTED_FOR_ASK_WITH_GRAY_REMEDIATION`, explicit 주군 ask approval `evt-004030`, KAH-wrapped GJC ULTRAGOAL setup evidence, implementation `evt-004037`, enhance-test `evt-004039`, optimize `evt-004041`, docs/status update `evt-004043`, official implementation review/request_feedback `evt-004045`, focused Orange remediation acceptance, handle_feedback `evt-004047`, and MAR Blue disposition/completion `evt-004052`. Local implementation adds `internal/daemon/prslr011_e2e_test.go` as the bounded local/default-off dialogue-quality harness proving two selected participant runner invocations, canonical selected-member `speech` linkage, `prior_speaker_argue_quality_evidence` coverage for the non-opening speech, moderator synthesis, transcript/projection/export preservation, and no runnerless repair. Acceptance artifacts are under `.kkachi/runs/run-20260706T144956Z-c4f49751f7f4/artifacts/acceptance/`; focused and broad control/plugin Make/KAT checks passed in `test-log.md`, with post-feedback verification in `post-feedback-verification.md`. KAH `second_color_review` completed at `evt-004059` after Red `t_529c1666`, Orange `t_019580e4`, Gray `t_776e2b69`, focused Orange `t_230f3cb0`, focused Gray `t_102d90f9`, and Blue `t_5564ae23`; final verification completed at `evt-004062` and improve/status closeout completed at `evt-004064`, workflow revision `27`, with no ready nodes remaining. This row does not claim live Discord rollout, production activation, runtime/profile/provider/gateway/auth/token/model mutation, package publication, push, or broad readiness. |
| PRSLR-012 | Visible delivery echo de-duplication | completed/local-proof-direct | Control-owned direct follow-up requested by 주군 after the 2026-07-07 live KLM audit, intentionally performed without Kkachi/KAH workflow. The implementation classifies runnerless `speech` events as `visible_delivery_echo_count` only when they carry posted Discord visible-message evidence for the same member/turn as a verified selected-runner canonical speech and the echo evidence either references that canonical speech event or matches its canonical delivery `message_id` on the expected surface. Such verified echoes are not independent runnerless/fallback speech. Discussion-quality status excludes verified visible echoes from canonical ARGUE speech counts and prior graph state, preserving `discussion_quality_pass` for the real canonical speech set while still exposing the echo count as diagnostics. Fail-closed boundary tests keep unselected runner speech, mismatched visible `message_id`, and unlinked visible speech in runnerless/fallback or canonical quality counts. Local verification includes focused PRSLR-012 visible echo accounting tests, discussion-quality count tests, selected-runner boundary regressions, focused storage regression tests, full control gates, and docs guardrails; no live rollout, daemon/profile/provider/gateway/auth/token/model mutation, push, package publication, or broad readiness is claimed. |
