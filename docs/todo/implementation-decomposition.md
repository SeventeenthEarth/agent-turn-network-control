# Implementation Epics

## Release v1 target scope

Release v1 covers the Go control daemon/CLI plus the compatible Python Hermes plugin adapter. This document records implementation decomposition and dependency shape. Current control epic/task status lives in `docs/roadmap.md`; plugin-specific roadmap status lives in `../../agent-turn-network-plugin/docs/roadmap.md`.

Control scope: registry, storage, daemon, CLI, protocol/conformance, stream, member runtime contract, runner adapter, delegation, review gate, council, transcript/export, distribution, reliability.

Plugin scope: Hermes plugin manifest/entrypoint, Python daemon client, tools/slash commands, bundled skill, Discord surface helper, plugin UX diagnostics, and plugin conformance tests.

## Epic and task ID convention

Epic IDs are normally five-letter uppercase English slugs. Task IDs normally use `{EPIC}-001`, `{EPIC}-002`, and so on. Explicit user-approved exceptions include `ATN`, `RUNFIX2`, `RUNFIX3`, and the six-letter `NEXFIX` epic whose requested task id prefix is `NEWFIX-*`. For a jointly developed control/plugin capability, both repositories use the same epic ID and a single globally sequential task number stream; cite tasks with repo-qualified notation such as `control/RUNFIX-001` or `plugin/RUNFIX-002`. A repository may therefore have gaps in its local task numbers when another repo owns the intervening task. The current Release v1 and post-Release epic IDs are:

| Epic ID | Epic Title | Scope |
| --- | --- | --- |
| BOOTS | Bootstrap | Go scaffold and local gates |
| REGST | Registry/security | identity and file-safety boundary |
| STORE | Storage/event SOT | session directory, event log, projection, replay |
| DAEMN | Daemon/CLI/protocol | daemon, command transport, stream, conformance |
| RUNRT | Runtime/runner | member runtime loop and bounded runner adapter |
| DELEG | Delegation/review | delegation lifecycle and review gates |
| COUNC | Council/consensus | council discussion, voting, and consensus state |
| TRANS | Transcript/distribution | transcript/export, distribution docs, plugin handoff |
| RELIA | Reliability/release | observability, disaster recovery, release acceptance |
| LTRAN | Live transport control compatibility | daemon/CLI compatibility reads and live-local support |
| MEMBR | Member runtime profile invocation | real participant profile/wrapper invocation proof |
| SURFD | Surface delivery evidence contract | event-to-visible rendering and evidence projection |
| ENSOT | Event/outcome visible-closeout SOT | terminal council outcome and moderator visible-closeout semantics |
| ARGUE | Council argument graph and discussion quality | control protocol/fixtures/validation/projection for claim-relation evidence |
| RUNFIX | ATN council runner, activation, and discussion-quality remediation | cross-repo remediation for selected-speaker runner dispatch, adapter command contract, activation UX, Discord/profile eligibility, fallback disclosure, and ARGUE quality proof |
| RUNFIX2 | ATN discussion runtime usability hardening | cross-repo hardening for production-default enablement semantics, selected-runner real response generation, discussion lifecycle closeout turns, clean Discord visible transcript UX, and integrated explicit discussion proof |
| ATN | Agent Turn Network public rename | clean five-task cross-repo public rename to Agent Turn Network with no legacy aliases |
| RUNFIX3 | Live-visible council contract hardening | post-ATN live-thread hardening for exact Discord thread binding, lifecycle closeouts, selected-runner evidence, content/audit separation, dialogue mode, and drift repair |
| NEXFIX | Selected-runner prompt envelope remediation | post-RUNFIX3 content-plane readiness hardening for projection-backed selected-runner prompts and moderator/participant missing-context guardrails; explicit exception uses `NEWFIX-*` task ids per 주군 request |
| LVCOR | Live-visible council one-pass reliability | post-Jooyoo request-changes hardening for parameterized full-lifecycle completion guards, terminal moderator synthesis/finalize proof, CLI/plugin schema alignment, timeout semantics, and one-pass acceptance proof |
| PRSLR | Persistent runtime session latency repair | persistent per-participant council sessions, all-member delta coverage, first-class drop, bounded response windows, delta/rehydrate-only prompt policy, and plugin evidence consumption |

## Epic dependency graph

| Epic ID | Depends on | Reason |
| --- | --- | --- |
| BOOTS | none | Go scaffold and test gates |
| REGST | BOOTS | identity and file-safety boundary |
| STORE | REGST | session snapshot and append require registry authority |
| DAEMN | REGST, STORE | command path, stream, active-session lock; protocol/conformance can start from BOOTS and is finalized here |
| RUNRT | REGST, STORE, DAEMN | stream and wrapper accounting prerequisites |
| DELEG | RUNRT | delegation lifecycle and review gates depend on runtime sessions |
| COUNC | DAEMN, RUNRT | council depends on stream-driven runtimes |
| TRANS | STORE, DELEG, COUNC | rendering depends on event log and collaboration events |
| RELIA | REGST, STORE, DAEMN, RUNRT, DELEG, COUNC, TRANS | full acceptance, recovery, observability |
| LTRAN | RELIA | live-local control compatibility starts after Release v1 local acceptance |
| MEMBR | LTRAN | real participant invocation needs live-local stream/control compatibility |
| SURFD | MEMBR, TRANS | visible rendering needs participant response evidence plus transcript/export surfaces |
| ENSOT | SURFD | plugin visible closeout UX needs a locked terminal outcome / visible evidence contract |
| ARGUE | ENSOT, COUNC, TRANS | discussion-quality relation evidence depends on council speech events, terminal visible-closeout semantics, and transcript/export surfaces |
| RUNFIX | MEMBR, ARGUE, SURFD, ENSOT, LTRAN | dogfood remediation depends on prior selected-participant proof seams, argument-graph protocol, visible-surface evidence, terminal closeout semantics, and live transport compatibility |
| RUNFIX2 | RUNFIX, MEMBR, ARGUE, SURFD, ENSOT, LTRAN | 실사용 dogfood feedback depends on existing RUNFIX accounting/readiness/activation proof and hardens the remaining selected-runner, terminal readiness, turn lifecycle, and visible UX gaps before any stronger live-local readiness claim |
| ATN | RUNFIX2 | ATN clean public rename starts after the current runtime usability hardening lane so naming churn does not mask readiness evidence; ATN-001 is docs-only and does not authorize code, binary, package, or runtime mutation |
| RUNFIX3 | RUNFIX2, ATN | 2026-06-25 post-rename live-thread postmortem depends on RUNFIX2 lifecycle/runner accounting and the completed ATN rename surfaces, then hardens the remaining live-visible thread contract without claiming live readiness. |
| NEXFIX | RUNFIX3, RUNFIX2, ARGUE, MEMBR | 2026-06-26 selected-runner prompt envelope defect depends on existing selected-runner dispatch, runtime readiness, discussion-quality evidence, and live-thread diagnostics, then hardens the content-plane prompt envelope without claiming live readiness. |
| LVCOR | RUNFIX3, NEXFIX | 2026-06-30 Jooyoo one-pass live-visible council request-changes depends on prior lifecycle formula, selected-runner prompt/runtime delivery hardening, agenda-context repair, and the later selected-runner golden-path stabilization evidence before adding stronger parameterized completion/finalization/operator guards. |
| PRSLR | LVCOR, COUNCIL-STAB, RUNFIX2, ARGUE, MEMBR | 2026-07-04 persistent participant runtime SOT depends on existing selected-runner, ARGUE, participant-runtime readiness, lifecycle, and live-visible completion evidence, then repairs the latency/context bug by replacing per-turn fresh/full-history invocation with council-scoped persistent participant sessions and delta/rehydrate-only recovery. |

## Implementation phase grouping

| Phase | Epic IDs | Primary output | Exit gate |
| --- | --- | --- | --- |
| Bootstrap | BOOTS | `go.mod`, `cmd/`, `internal/`, `Makefile`, docs guardrails, help smoke tests | `make test` passes without external resources |
| Phase 1 | REGST | data-home and registry validation | registry/security tests pass |
| Phase 2 | STORE | `channel.jsonl`, SQLite projection, replay | append/replay/projection tests pass |
| Phase 3 | DAEMN | daemon, CLI, stream, structured errors, conformance fixtures | CLI integration + conformance tests pass |
| Phase 4 | RUNRT | member runtime loop contract and bounded `hermes-agent` runner | fake runner/runtime tests pass |
| Phase 5 | DELEG | delegation and review gate plus plugin-consumable fixture handoff | delegation/review E2E via CLI/fakes and conformance fixture checks pass |
| Phase 6 | COUNC | council and consensus | council/consensus tests pass |
| Phase 7 | TRANS | transcript/export/distribution docs | golden transcript + install docs pass |
| Phase 8 | RELIA | reliability, observability, disaster recovery | full Release v1 acceptance suite pass |
| Phase 9 | LTRAN | live-local daemon/CLI compatibility for plugin transport | disposable control live-local evidence and compatibility checks pass |
| Phase 10 | MEMBR | real participant profile/wrapper invocation path | selected participant invocation evidence passes without role substitution |
| Phase 11 | SURFD | surface delivery evidence contract | projection/transcript/export or fixture evidence supports visible rendering |
| Phase 12 | ENSOT | terminal outcome / visible closeout event semantics | docs SOT accepted after Red/Orange/Gray review and Blue synthesis |
| Phase 13 | ARGUE | council argument graph and discussion-quality evidence | ARGUE-001 accepted after Red/Orange/Gray review and Blue synthesis; ARGUE-002 static protocol/fixture scope accepted under KAS/KAH run `run-20260615T145822Z-caab064cf550`; ARGUE-003+ require separate explicit authorization |
| Phase 14 | RUNFIX | council runner, activation, and discussion-quality remediation | RUNFIX-001/RUNFIX-002 docs-only SOT locks are accepted after Red/Orange/Gray review, focused re-check, and Blue final synthesis; RUNFIX-003 through RUNFIX-011 are completed within their repo-qualified bounded scopes, with RUNFIX-010 classified as PASS_WITH_RISK and RUNFIX-011 limited to local implementation proof rather than production/live readiness; plugin/RUNFIX-012 and plugin/RUNFIX-013 have local implementation proof in the plugin lane; control/RUNFIX-014 has local implementation proof for selected-runner terminal accounting under KAH run `run-20260619T051710Z-8e1f6efb61ec`; plugin/RUNFIX-015 has local implementation proof for pure/local visible author identity guard planning under KAH run `run-20260619T071526Z-7d2ba33b07d5`; control/RUNFIX-016 has local implementation proof for summary schema robustness under KAH run `run-20260619T083649Z-d10e1f5cc20b` and local commit `9c15d22`; plugin/RUNFIX-017 has local implementation proof in the plugin lane; control/RUNFIX-018 has local implementation proof for daemon registry reconciliation of explicit council rosters under KAH run `run-20260619T214003Z-8a2afe33923f`; plugin/RUNFIX-019 consumes that evidence in activation planner/operator guidance under KAH run `run-20260619T214004Z-4d1e54b7304a`; no profile/provider/gateway/auth/token mutation, push, production activation, live delivery, or broad rollout is authorized. |
| Phase 15 | RUNFIX2 | discussion runtime usability hardening | Five-task cross-repo hardening sequence: evidence/config semantics and terminal readiness model; selected-runner Hermes response-generation adapter fix; discussion lifecycle with moderator intro, participant discussion turns, participant closeouts, and moderator conclusion; clean visible Discord transcript/audit separation; integrated explicit discussion proof. RUNFIX2 does not force evidence labels true and does not authorize profile/provider/gateway/auth/token mutation, live pilot success, live Discord delivery, production activation, push, or broad rollout. |
| Phase 16 | ATN | Agent Turn Network public rename | ATN-001 locks SOT/roadmap/docs-map policy, ATN-002/003 scrub public docs by repo, ATN-004 renames control code/binaries/protocol, and ATN-005 renames plugin package/tools/skills plus final guardrails. ATN-001 does not authorize live/profile/provider/gateway/auth/token mutation, hosted rename, push, package publication, or production rollout. |
| Phase 17 | RUNFIX3 | live-visible council contract hardening | RUNFIX3-001 locks the 2026-06-25 SOT and roadmap state after Red/Orange/Gray review and Blue synthesis; RUNFIX3-002/003 are plugin-owned guidance/planner evidence tasks; RUNFIX3-004 is control-owned diagnostics/enforcement follow-up. No production activation, live Discord rollout, profile/provider/gateway/auth/token/model mutation, push, or broad rollout is authorized. |
| Phase 18 | NEXFIX | selected-runner prompt envelope and live-visible delivery remediation | NEWFIX-001 is the control-owned blocking fix for projection-backed selected-runner prompt construction, prompt-content regression tests, and missing-agenda fail-closed diagnostics; NEWFIX-002 is plugin-owned operator guidance hardening; NEWFIX-003 locks the 2026-06-28 live-visible runtime delivery diagnosis and task split; NEWFIX-004 remains implemented with review pending for selected-member own-history prompt context, NEWFIX-005 remains implemented with review pending for live-visible `dispatch_timeout_sec=120` policy/evidence across creation, status/export, and drift diagnostics, and NEWFIX-006 remains implemented with review pending for the plugin planner/operator guardrail consuming those control evidence contracts; NEWFIX-007 is completed for control CLI/fixture required-context repair; NEWFIX-008 is completed for plugin `command.submit` representation/idempotency repair after MAR arch status-surface remediation, focused arch re-review, and Blue MAR disposition; NEWFIX-009 is completed for the cross-repo agenda-context acceptance proof after Orange/Gray request-changes remediation. The epic did not by itself authorize live Discord rollout, default Discord activation, daemon startup/discovery or mutation, production activation, profile/provider/gateway/auth/token/model mutation, package publication, push, or broad rollout; the later default-setting update consumes concrete Jooyoo live-success evidence while preserving separate production/runtime mutation gates. |
| Phase 19 | LVCOR | live-visible council one-pass reliability | Five PR-sized request-changes tasks from Jooyoo's 2026-06-30 one-pass live-visible council reliability report: dynamic full-lifecycle accounting guard; parameterized terminal moderator synthesis/finalize proof; CLI/daemon schema alignment and timeout semantics; plugin lifecycle envelope golden tests; parameterized full-shape acceptance proof. LVCOR did not by itself authorize production activation, live Discord rollout, default Discord activation, daemon/profile/provider/gateway/auth/token/model mutation, package publication, push, or broad rollout; the default-setting update now promotes Discord-origin decision councils to the standard live-visible lifecycle using LVCOR/NEWFIX evidence while keeping production readiness and runtime mutation separately approval-gated. |
| Phase 20 | PRSLR | persistent runtime session latency repair | Seven-task cross-repo sequence including plan-vet: plan lock; control drop protocol; control response window/replay; control persistent session registry and all-member observe coverage; control delta prompt/rehydrate fail-close; plugin command/evidence-surface alignment; cross-repo acceptance/docs closeout. PRSLR does not authorize live Discord rollout, production activation, profile/provider/gateway/auth/token mutation, push, or broad readiness. |

## BOOTS — Bootstrap

| Task ID | Scope | Suggested paths | Verification |
| --- | --- | --- | --- |
| BOOTS-001 | Create `go.mod`, `cmd/atn-control`, `cmd/atn-controld`, initial `internal/protocol`, test layout, and Makefile. | `cmd/`, `internal/`, `tests/`, `Makefile` | `make test`, binary `--help` exits 0, no external resources used. |

## REGST — Registry/security

- Define registry schema for `<data_home>/registry.yaml`.
- Implement data-home resolution: `$ATN_HOME` > `$XDG_DATA_HOME/agent-turn-network` > `~/.atn/`.
- Validate safe data-home and registry permissions.
- Load registry through TOCTOU-reduced file handling.
- Reject reserved principals: `user`, `atn-controld`.
- Validate wrapper paths and env allowlist.
- Reconcile explicit approved council rosters into the daemon registry only when principal mapping and same-named wrapper resolution are unambiguous; otherwise fail closed before session creation.
- Write per-session `registry_snapshot.yaml` before `session_created`.
- Add `registry validate` and `registry show` CLI commands.

## STORE — Storage/event SOT

- Create session directories.
- Append canonical event envelopes to `channel.jsonl`.
- Maintain SQLite projection as rebuildable cache.
- Store registry snapshot metadata.
- Project recipients, runner invocations, escalation batches, Discord surface metadata, linked authority metadata.
- Rebuild projection from event log deterministically.

## DAEMN — Daemon/CLI/protocol

- Implement `atn-controld` process and local command transport.
- Implement `atn-control` CLI commands that talk to the daemon.
- Implement stream replay/follow/cursor acknowledgement.
- Implement single active-session lock.
- Implement structured JSON errors and stable exit categories.
- Add status, doctor, storage verify/rebuild, block/resume, limits extension.
- Define command envelope, stream frame, structured error, and version/feature schemas.
- Add conformance fixtures under `testdata/conformance/`.
- Add tests proving CLI and daemon obey the same contract.
- Publish compatibility guidance for the plugin repo.
- Forbid shared-source assumptions; compatibility is through protocol and tests.

## DELEG — Delegation/review

| Task ID | Scope | Suggested paths | Verification |
| --- | --- | --- | --- |
| DELEG-001 | Implement delegation lifecycle, review request/response gates, blocked/resume handling, CLI/E2E fake coverage, and audit evidence. | `internal/`, `cmd/`, `tests/`, `docs/spec/protocol-and-cli.md`, `docs/spec/protocol-and-cli.md`, `docs/spec/architecture.md`, `docs/spec/operations.md` | delegation/review local CLI and fake E2E checks, `make check-plugin-contract`, `make test` |
| DELEG-002 | Publish plugin-consumable delegation/review command and structured-error fixtures for success, duplicate/idempotency, permission/error, retryable failure policy, and malformed-response handling. | `testdata/conformance/`, conformance validation tests, `docs/spec/protocol-and-cli.md`, `docs/spec/cross-repo-contract.md`, `docs/spec/testing-and-tooling.md` | manifest/fixture validation, `make check-plugin-contract`, `make test`, downstream `make check-core-contract` from the plugin repo |

## RUNRT / COUNC / TRANS / RELIA summary

Remaining summarized Release v1 epics preserve the previous product behavior: member runtime and runner adapter, council discussion, consensus, transcript/export, distribution, observability, disaster recovery, and full testing. Each epic must include unit, integration, and, when appropriate, isolated E2E tests mapped to the Makefile target contract in `docs/spec/testing-and-tooling.md`.

## Post-Release live-local epics

These epics are planned after Release v1 local acceptance. They are repo-owned control epics that gate companion plugin epics in `../../agent-turn-network-plugin/docs/roadmap.md`.

Active task transfer between control and plugin happens only at an epic boundary. Do not interrupt a control epic with plugin tasks, and do not interrupt a plugin epic with control tasks. If a missing sibling capability is found, block the active epic with evidence and complete the sibling epic that owns the missing capability before resuming.

| Task ID | Scope | Suggested paths | Verification |
| --- | --- | --- | --- |
| LTRAN-001 | Add the control companion live transport SOT, roadmap entries, docs-map/index updates, and cross-repo handoff rule. This is docs-only SOT/mapping work and does not unblock plugin live transport by itself. | `docs/spec/live-transport-control-sot.md`, `docs/roadmap.md`, `docs/README.md`, `docs/kkachi-docs-map.yaml`, `docs/spec/cross-repo-contract.md` | `make docs-guardrails`, `make check-plugin-contract`, `make test-prepare`; plugin `make check-core-contract` when practical |
| LTRAN-002 | Confirm or add daemon compatibility read shapes needed by plugin live transport. | `internal/daemon/`, `internal/command/`, `internal/protocol/`, `testdata/conformance/`, `docs/spec/protocol-and-cli.md`, `docs/spec/protocol-and-cli.md` | status/version/health/stream/council command tests, conformance checks, `make test` |
| LTRAN-003 | Prove disposable CLI/daemon live-local support for plugin equivalence. | CLI integration tests, conformance fixtures, release/local scripts, docs evidence | disposable data-home smoke, command-id/idempotency checks, stream replay/follow/ack checks |
| MEMBR-001 | Select and document first participant invocation pilot mode and evidence rules. The first pilot is main-agent mediated bounded runner invocation as a disposable local proof before long-lived member runtimes; it preserves real profile/wrapper identity and durable runner/session evidence, and forbids role substitution. | `docs/adr/runtime-decisions.md`, `docs/spec/live-transport-control-sot.md`, runtime docs/evidence | docs guardrails and review acceptance |
| MEMBR-002 | Prove selected participant invocation and durable success/failure event recording. Isolated fake-wrapper implementation evidence is Blue-accepted as candidate proof; real-profile/live invocation remains unproven and approval-gated. | `internal/daemon/`, `internal/memberruntime/`, `internal/runner/`, CLI/runtime tests, docs | fake/isolated wrapper tests accepted as candidate proof; real-profile evidence only when explicitly authorized; no role substitution |
| SURFD-001 | Define event-to-visible-surface rendering/evidence contract. | `docs/spec/protocol-and-cli.md`, `docs/spec/architecture.md`, `docs/spec/operations.md`, `docs/spec/live-transport-control-sot.md` | docs guardrails, protocol consistency checks |
| SURFD-002 | Prove projection/transcript/export or fixture evidence for visible rendering tests. | `internal/transcript/`, `internal/storage/`, `testdata/conformance/`, docs | transcript/export/projection tests, delivery evidence fixture checks, `make test` |
| ENSOT-001 | Lock council terminal outcome visible-closeout SOT for plugin `VISUX` implementation. Drafts and votes are visible process milestones only; terminal daemon events are durable outcomes only; human-readable moderator closeout requires posted delivery/projection evidence and fails closed when missing, failed, pending, or mismatched. | `docs/spec/protocol-and-cli.md`, `docs/spec/architecture.md`, `docs/spec/operations.md`, `docs/spec/live-transport-control-sot.md` | docs guardrails, protocol consistency checks, ATN Red/Orange/Gray review, Blue synthesis |
| ARGUE-001 | Close out the council argument graph SOT, docs index, docs map, implementation epic, and roadmap links. Accepted/completed docs-only after official ATN Red `t_4a2e735f`, Orange `t_9f4b2b9c`, Gray `t_b196d630`, and Blue synthesis. | `docs/spec/council-argument-graph-sot.md`, `docs/README.md`, `docs/roadmap.md`, `docs/todo/implementation-decomposition.md`, `docs/kkachi-docs-map.yaml` | `make docs-guardrails`, `make check-plugin-contract`, ATN Red/Orange/Gray review, Blue synthesis |
| ARGUE-002 | Define additive protocol shape and publish control-owned static conformance fixtures for claim/relation evidence. Accepted for bounded local static protocol/fixture scope under KAS/KAH run `run-20260615T145822Z-caab064cf550` after Red `t_e2ced3fc`, Orange `t_fd35e83a`, Gray `t_c9e20348`, Blue synthesis `t_ade91c69`, and final gate `evt-001437`. This slice does not implement runtime validation/scoring, transcript/export rendering, or plugin behavior. | `docs/spec/protocol-and-cli.md`, `testdata/conformance/`, `internal/protocol/conformance_test.go`, `scripts/check_plugin_contract.py` | `go test ./internal/protocol`, `make check-plugin-contract`, `make test-prepare`, `git diff --check`; plugin consumes fixtures in its own repo |
| ARGUE-003 | Implement daemon/CLI validation and moderator scoring hooks for quality-required and quality-warn modes. Completed as a local Stage 1 implementation under KAS/KAH run `run-20260615T181228Z-b79cfade404a` after implementation verification, color review, Octo review, post-Octo re-review, final KAH gate, and commit approval. | `internal/daemon/`, `internal/protocol/`, `internal/storage/`, CLI tests | focused validation/scoring tests, `make test` |
| ARGUE-004 | Preserve relation evidence in transcript/export/projection surfaces. Completed as a bounded local Stage 1 implementation under KAS/KAH run `run-20260616T073755Z-f2fe201156c7` after transcript/export/projection tests, first color review, Red R1 color-adjust, focused Red re-review, official KAB GLM Octo review, post-Octo Red/Orange/Gray re-review, and Blue synthesis. This remains control-local evidence only; plugin JSON-string parsing, missing-field transcript UX handling, live readiness, production activation, commit/push, and pilot readiness remain separate gates. | `internal/transcript/`, `internal/storage/`, projection/export tests | golden transcript/export/projection tests, `make test` |
| ARGUE-005 | Prove the control integration gate before plugin handoff or live-local pilot planning. Completed as a local verification gate under KAS/KAH run `run-20260616T132731Z-781418864c04` after plugin-contract compatibility, ARGUE conformance fixture, validation/scoring, transcript/export/projection, and full local test evidence passed. This does not claim plugin/ARGUE-004, live-local pilot readiness, production activation, or live Discord/profile/provider mutation. | integration tests, conformance checks, docs evidence | `make check-plugin-contract`; focused `go test ./internal/protocol`; focused `go test ./internal/storage`; full `make test`; no live activation |

Plugin-side ARGUE adapter, rendering, participant response generation, and packaged operator guidance are companion consumer work in `../../agent-turn-network-plugin/`. They are referenced here only as handoff/consumer notes and are not control-roadmap tasks.


## RUNFIX — ATN council runner, activation, and discussion-quality remediation

`RUNFIX` is a cross-repo remediation epic created from the 2026-06-17 ATN council dogfood issue report. The epic uses one global task number stream across the control and plugin repositories. The owning repository is part of the task citation; gaps in a repo-local list are expected and intentional.

RUNFIX does not assert production/live council readiness. `plugin/RUNFIX-010` provides bounded PASS_WITH_RISK live-local evidence and operator guardrails only. Operators must distinguish lifecycle-only success, manual/fallback profile success, selected-speaker runner success, visible-surface success, ARGUE discussion-quality success, and Discord live-visible turn output.

Legacy pre-ATN labels that remain in older RUNFIX rows are historical/provenance labels only. They are not current public ATN aliases.

| Global Order | Repo | Task ID | Scope | Suggested paths | Verification / gate |
|---:|---|---|---|---|---|
| 1 | control | RUNFIX-001 | Lock the control-side remediation SOT, readiness/fallback labels, cross-repo DAG, and roadmap entries. | `docs/spec/live-transport-control-sot.md`, `docs/todo/implementation-decomposition.md`, `docs/roadmap.md`, `docs/kkachi-docs-map.yaml` | docs guardrails, plugin contract check, Red/Orange/Gray review, Blue synthesis |
| 2 | plugin | RUNFIX-002 | Lock the plugin-side activation/operator SOT, control dependency, cross-repo DAG, and roadmap entries. | plugin `docs/spec/live-transport-sot.md`, `docs/roadmap.md`, `docs/spec/skill-and-operator-guide.md`, bundled `atn-plugin` skill | plugin docs/test-prep/core-contract checks, Red/Orange/Gray review, Blue synthesis |
| 3 | control | RUNFIX-003 | Wire automatic selected-speaker member runtime dispatch so `speaker_selected` can trigger selected-member runner invocation without a custom harness. | `internal/daemon/`, `internal/memberruntime/`, `internal/runner/`, stream/cursor tests | disposable runner smoke; no profile/provider/gateway mutation |
| 4 | control | RUNFIX-004 | Correct Hermes adapter response-generation command contract and runner diagnostics. | `internal/runner/`, registry/runtime docs, runner tests | distinguishes adapter mismatch/model failure/timeout; no stale phase evidence |
| 5 | control | RUNFIX-005 | Completed/local-control: ARGUE/moderator quality status now separates lifecycle pass from `discussion_quality`, exposes quality diagnostics and hard warnings, keeps `quality_required` fail-closed for missing required relation evidence after the opening window, accepts `quality_warn` without text mutation or inferred durable links, and adds linked hand-raise `graph_need` counts. KAH run `run-20260618T020120Z-fe2144618fe6`. | `internal/storage/`, protocol/ARGUE docs/tests | Local evidence passed `git diff --check`, focused storage/protocol/daemon/command tests, `make docs-guardrails`, `make check-plugin-contract`, `make test-prepare`, and `make test`; Red `t_1d5692f1`, Orange `t_388bb347`, Gray `t_6fb40282`, and Blue synthesis `t_1eb87c6b` accepted bounded local-control closeout; no live Discord delivery, production daemon activation, profile/provider/gateway/auth/token mutation, plugin implementation/readiness, commit, push, or broad rollout claim |
| 6 | plugin | RUNFIX-006 | Local implementation proof: plugin run `run-20260618T045937Z-2e173b8309f3` added pure/local `atn_discussion_activation_plan` for explicit control/RUNFIX-005 dependency, plugin install/tool visibility, daemon config/compatibility evidence, participant profile eligibility, parent-channel inheritance proof, approval gates, rollback, blockers, and separated RUNFIX evidence labels. | plugin activation docs/tools; `atn_discussion_activation_plan` | dry-run reports planned changes, eligible/excluded/blocked profiles, rollback, approvals, blockers, and `live_readiness: false`; no apply/live-local pilot, live Discord delivery, daemon startup, profile/provider/gateway/auth/token mutation, production activation, commit, push, or broad rollout claim |
| 7 | plugin | RUNFIX-007 | Local implementation proof: plugin run `run-20260618T081811Z-23d10e2a4634` extends the existing pure/local `atn_discussion_activation_plan` for explicit effective Discord eligibility evidence, eligible-only allow-list targets, excluded/blocked profile remediation, parent-channel inheritance proof state, thread-only/current-channel/manual proof rejection, fallback audit, and `live_readiness: false`. | plugin docs/tools; `atn_discussion_activation_plan` | bot-to-bot enabled profiles excluded; unknown visibility/eligibility blocks; allow-list targets include eligible profiles only; no live Discord delivery, daemon startup/discovery, profile/gateway/provider/auth/token/model mutation, production activation, commit, push, or broad rollout claim |
| 8 | plugin | RUNFIX-008 | Local implementation proof: plugin run `run-20260618T092359Z-401d6e5bedc0` updates the pure/local activation planner, operator guide, bundled skill, and schema-visible planner contract so participant responses expose `speech`, `claims[]`, `stance_links[]`, `contribution_type`, `new_axis_reason`, optional `evidence[]`, ARGUE counts, selected-runner evidence, canonical `speaker_selected -> speech` linkage, and diagnostic-only fallback disclosure. | plugin activation planner sources, plugin schema sources, plugin `docs/spec/skill-and-operator-guide.md`, plugin bundled `atn-plugin` skill, plugin `docs/spec/live-transport-sot.md` | Local-only evidence passed focused planner/schema/bundled tests, `make docs-guardrails`, `make check-core-contract`, `git diff --check`, `make test-prepare`, and full plugin `make test` (329 unit / 64 integration / 13 e2e). Red `t_30b22678` accepted; Orange `t_381805a1` accepted with UX risk about explicit artifact path citation; Gray `t_6cb777d2` requested cross-repo status reconciliation and focused Gray `t_d65b0a83` accepted that reconciliation. No live Discord delivery, daemon startup/discovery, profile/provider/gateway/auth/token/model mutation, production activation, commit, push, or broad rollout claim. |
| 9 | control | RUNFIX-009 | Local implementation proof: control KAH run `run-20260618T102752Z-7d8ccfa584e4` adds an integrated daemon smoke fixture and transcript summary fix so durable local evidence covers runner invocation, canonical `speaker_selected -> speech` linkage, ARGUE `quality_warn` diagnostics/hard-warning exposure, and transcript/export/projection closeout evidence. | `internal/daemon/runfix009_smoke_test.go`, `internal/storage/transcript.go`, transcript/export/projection evidence | Local-only evidence passed focused RUNFIX-009 daemon smoke, related selected-speaker/export tests, storage package tests, `git diff --check`, `make docs-guardrails`, `make check-plugin-contract`, `make test-prepare`, and `make test`; Red plan review `t_3e1a6ecd` accepted scope. No live Discord delivery, production daemon activation, profile/provider/gateway/auth/token mutation, plugin/RUNFIX-010 execution, commit, push, or broad rollout claim. |
| 10 | plugin | RUNFIX-010 | Completed/PASS_WITH_RISK: ran approved bounded visible-local activation pilot and final operator package, then added Discord-origin live-visible default and unified non-visible override guardrails. | plugin activation/runbook/evidence | thread-preferred visible pilot policy with approved parent-channel fallback when thread creation/posting is unsupported; Discord-origin requests use `live_visible_thread` as the required default user-facing surface; non-visible modes (`artifact_only`, `daemon_cli_actor_speech`, transcript/export aliases, `activation_planning_only`, and local-daemon-only aliases) require supported `requested_output_mode`, `explicit_non_visible_override=true`, and non-empty `override_reason`; final reports must separate lifecycle finalized, Discord visible turns posted, real profile/gateway replies, and CLI actor speech only; final readiness classification is bounded PASS_WITH_RISK, not production/live readiness |
| 11 | control | RUNFIX-011 | Local implementation proof: control KAH run `run-20260618T162156Z-419f3769f2cc` implements derived participant runtime readiness and Discord-thread attendance/preparation preflight so councils fail closed instead of silently advancing without required subscribers/cursors/heartbeats or timeout/failure evidence. | `internal/storage/runtime_readiness.go`, `internal/daemon/council_handlers.go`, stream/status diagnostics, control RUNFIX docs | Focused storage/daemon tests cover subscriber/cursor/heartbeat readiness, missing/stale fail-closed cases, attendance and preparation timeout diagnostics, selected-runner prerequisite diagnostics, and no gateway/profile/provider/auth/token mutation. Local proof only; plugin/RUNFIX-012 and live readiness remain out of scope. |
| 12 | plugin | RUNFIX-012 | Local implementation proof: plugin KAH run `run-20260618T231811Z-ee5c6394d1fe` consumes `control/RUNFIX-011` readiness evidence in the plugin-owned activation planner/operator guardrails and reconciles plugin RUNFIX docs/status wording. | plugin `activation_planner`, schemas, operator guide, bundled skill, plugin RUNFIX SOT | Repo-qualified plugin-owned proof only; control docs record cross-repo traceability. `live_readiness=false` unless runtime subscriber/cursor/attendance/selected-runner/visible-surface evidence is explicit; no transcript/export-only or gateway-only success promotion, live Discord delivery, production/live readiness, profile/provider/gateway/auth/token/model mutation, commit, push, broad rollout, or control implementation claim. |
| 13 | plugin | RUNFIX-013 | Local implementation proof: plugin KAH run `run-20260619T001719Z-9c41001040ef` adds ATN council moderation hard rules from 주유's live moderation gap so moderators cannot run a Discord-visible simulated debate instead of a daemon-governed council. | plugin operator guide/bundled `atn-plugin` skill plus control moderator-policy references | lifecycle-first checklist; no predeclared complete live speaker order; per-turn poll/hand-raise/speaker_selected evidence; default `relevance` selection with per-turn justified `targeted`, `random`, `moderator_direct`, and `role_order`; Discord/Hermes reply is not state without daemon `speech`; moderator opinions use participant-style turns; cancel/restart before speech and repair-forward after speech; docs/bundled-skill/package proof only, with no live activation, Discord delivery, production/live readiness, profile/provider/gateway/auth/token/model mutation, commit, push, or broad rollout claim |
| 14 | control | RUNFIX-014 | Local implementation proof: control KAH run `run-20260619T051710Z-8e1f6efb61ec` adds selected-runner terminal accounting and live-report guardrails from 주유's 2026-06-19 rename-council harness feedback. Final reports distinguish lifecycle success from selected-speaker runner success when `runner_invocation_failed` appears before a later fallback/manual canonical `speech`, and expose runner started/succeeded/failed counts plus fallback/manual harness use before any live-readiness or automatic-runner claim. | storage selected-runner accounting helper, transcript/status/export/report helpers, protocol fixture | focused RUNFIX-014 storage tests, storage/protocol package tests, `git diff --check`, `make docs-guardrails`, `make check-plugin-contract`, `make test-prepare`, `make test`, CodeGraph refresh gate, Red/Orange/Gray review, and KAH final gate passed; no profile/provider/gateway/auth/token mutation, Discord delivery, production/live readiness, push, or broad rollout claim |
| 15 | plugin | RUNFIX-015 | Local implementation proof: plugin KAH run `run-20260619T071526Z-7d2ba33b07d5` adds pure/local `atn_discussion_activation_plan` visible-author guard evidence for pre-`council.new` planning. The plugin validates explicit caller-provided same-path probes, expected author source (`registry_snapshot` or approved profile-author map), observed bot/user, source-env/posting-path evidence, shared-default-then-profile-local env precedence, per-turn Discord message/member/author/speech linkage, and separated final report fields. Missing/shared-default/unexpected evidence fails closed without live Discord delivery or runtime/profile/provider/gateway/auth/token/model mutation. | plugin activation planner/schema/tests; operator guide/bundled skill | profile env precedence test: shared defaults first, profile-local env overrides; per-turn message id/member/author/speech linkage evidence; no hidden fallback |
| 16 | control | RUNFIX-016 | Local implementation proof: control KAH run `run-20260619T083649Z-d10e1f5cc20b` final gate passed and local commit `9c15d22` adds the canonical `internal/storage/summary_turn_accounting.go` helper for `channel.jsonl` and export bundles plus export manifest key `summary_turn_accounting`. It emits stable turn/member/selected/speech/runner/visible-message rows with `speaker_selected_event_id`, `speech_event_id`, `runner_invocation_id`, and visible message id where available, and tolerates `payload.plugin_evidence` objects, explicit visible/Discord evidence objects/lists, missing optional evidence after `council_finalized`, and unsupported evidence maps/lists without crashing. Unsupported arbitrary maps/lists remain non-proof and cannot promote visible delivery proof, `selected_runner_pass`, live readiness, plugin readiness, or production readiness. | `internal/storage/summary_turn_accounting.go`, export/transcript/storage helpers, storage/report tests | focused RUNFIX-016/RUNFIX-014/SURFD-002 storage tests, full `internal/storage`, `git diff --check`, `make test`, `make docs-guardrails`, `make check-plugin-contract`, and plugin `make check-core-contract` passed in run artifacts; official color/final gate passed |
| 17 | plugin | RUNFIX-017 | Plugin-owned local implementation proof: plugin KAH run `run-20260619T101255Z-189d01ba8b8f` hardens ARGUE quality-required prompt and runner contracts after official color review, Blue synthesis, and final KAH gate. Participant prompts and selected-response handling must provide compact prior claim graph targets, require non-opening speeches to include `stance_links[]` or justified `new_axis`, preserve ARGUE fields, and fail `discussion_quality_pass` on the first orphan non-opening speech in `quality_required` pilots and expose repeated-orphan counts without synthetic relation inference. | plugin selected-response guidance/tools, operator guide, bundled skill; control/RUNFIX-005 dependency | quality-required prompt/response fixtures; final reports split lifecycle and discussion-quality counts; no synthetic relation inference; no control-owned implementation authority claim from this control traceability row |
| 18 | control | RUNFIX-018 | Local implementation proof: control KAH run `run-20260619T214003Z-8a2afe33923f` implements daemon-owned registry reconciliation for explicit `council.new` rosters. Missing selected moderator/participant principals are added to persistent `registry.yaml` only when ids are valid/non-reserved and same-named wrapper resolution is unambiguous; disabled existing principals, invalid ids, unresolved wrappers, or missing registry evidence fail closed before session creation. The daemon reloads the registry, snapshots it, and returns `registry_reconcile` evidence. | `internal/registry/reconcile.go`, `internal/daemon/council_handlers.go`, registry/daemon tests, SOT docs | focused registry/storage/daemon tests; full test/docs gates before commit-ready; no live daemon/profile/gateway/auth/token mutation |

RUNFIX implementation tasks after accepted RUNFIX-001/RUNFIX-002 docs-only SOT locks require separate 주군 authorization before implementation starts. `RUNFIX-003` through `RUNFIX-011` are completed within their repo-qualified bounded local/control scopes; `plugin/RUNFIX-012` is completed as plugin-owned local implementation proof under KAH run `run-20260618T231811Z-ee5c6394d1fe`, and `plugin/RUNFIX-013` now has plugin-owned local implementation proof under KAH run `run-20260619T001719Z-9c41001040ef` for packaged moderation hard rules only. `control/RUNFIX-014` has local implementation proof under KAH run `run-20260619T051710Z-8e1f6efb61ec` for selected-runner terminal accounting and fallback/manual harness disclosure. `control/RUNFIX-016` has local implementation proof under KAH run `run-20260619T083649Z-d10e1f5cc20b` and local commit `9c15d22` for summary/turn-accounting robustness. Completed proofs still do not authorize production activation, live readiness, broad rollout, Discord delivery, or profile/provider/gateway/auth/token/model mutation. `plugin/RUNFIX-010` completed with bounded PASS_WITH_RISK visible-local pilot evidence in KAH run `run-20260618T112843Z-40b023a5d9c8` and now includes the live-visible/default-output guardrail; remaining limitations are parent-channel fallback is not no-restart thread readiness, `real_profile_live_invocation=false`, and full HUN team roster coverage is unproven. `plugin/RUNFIX-015` now has plugin-owned local implementation proof under KAH run `run-20260619T071526Z-7d2ba33b07d5` for pure/local visible author guard planning. `plugin/RUNFIX-017` has plugin-owned local implementation proof under plugin KAH run `run-20260619T101255Z-189d01ba8b8f` after official color review, Blue synthesis, and final KAH gate; `control/RUNFIX-018` has local implementation proof under KAH run `run-20260619T214003Z-8a2afe33923f` for daemon registry reconciliation of explicit rosters; `plugin/RUNFIX-019` consumes that evidence in plugin activation ... [truncated]


## RUNFIX2 — ATN discussion runtime usability hardening

`RUNFIX2` is the 2026-06-20 follow-up epic from the KLM/주유 live discussion dogfood. It preserves the `RUNFIX` lineage even though the epic id is longer than the usual short slug, because the work is a continuation of the same live-council readiness lane and must remain traceable to `RUNFIX` evidence labels.

RUNFIX2 uses five intended PR-sized tasks. The pass labels remain evidence-derived and fail closed: no task may force `visible_surface_pass`, `participant_runtime_live_ready`, `selected_runner_pass`, or `discussion_quality_pass` to true without matching durable event/evidence. Production/operator defaults may enable or require live-visible surface, participant runtime readiness, and selected-runner execution, but those enablement settings are distinct from final evidence labels.

| Global Order | Repo | Task ID | Scope | Suggested paths | Verification / gate |
|---:|---|---|---|---|---|
| 1 | control | RUNFIX2-001 | Completed/local-control: separate production/default enablement from evidence-derived pass labels and fix terminal-session readiness semantics so completed councils are judged by readiness at grant/turn time rather than stale post-final heartbeat alone. Local code now exposes `generated_at` separately from `evaluated_at` / `evaluation_mode` / freshness-reference fields and preserves `*_pass` evidence derivation. KAH final gates, official Red/Orange/Gray review, and Blue acceptance passed; push/live activation remain separately approval-bound. | `internal/storage/runtime_readiness.go`, `internal/storage/stream.go`, `internal/storage/runtime_readiness_test.go`, `docs/spec/protocol-and-cli.md`, `docs/spec/live-transport-control-sot.md` | readiness/report regressions; docs guardrails; plugin contract/core checks; Red/Orange/Gray |
| 2 | control | RUNFIX2-002 | Completed/local-control: selected-runner Hermes adapter default invocation now uses response-generation `chat -Q -q <prompt>` instead of delivery-only `send <prompt>`, daemon dispatch records `runner_invocation_succeeded` plus linked canonical `speech`, selected-runner stdout is standardized as compact JSONL with `payload.speech`, real Hermes CLI `session_id: ...` control lines are ignored, pretty/multiline JSON and message/content/text-to-speech normalization are accepted only as compatibility input, CLI `council.grant` uses an extended timeout for selected-runner generation, and terminal delivery/fallback mismatch evidence remains fail-closed. | `internal/runner/`, `internal/daemon/`, `internal/command/`, `internal/storage/`, fake/isolated wrapper fixtures, `docs/spec/operations.md` | fake/isolated wrapper and stdout-framing tests passed; direct real-profile `jangbi chat -Q -q` stdout probe confirmed the `session_id:`-before-JSON shape and local selected-runner smoke exposed message-to-speech compatibility, now covered in daemon tests; command timeout policy covered; no role substitution; no hidden fallback; no live provider/gateway/auth/token mutation |
| 3 | control | RUNFIX2-003 | Completed/local-control: explicit `limits.max_discussion_turns` lifecycle accounting now reports T0 moderator opening, T1..Tmax selected participant discussion turns, one selected closeout speech per participant, and separated terminal conclusion visibility. `council.propose` fails closed until the configured discussion window and participant closeouts are present; lifecycle accounting only counts a speech after an earlier same-turn same-member `speaker_selected` in log order, so later grants do not retroactively satisfy prior raw speeches. `council.unresolved` remains available as the fail-closed terminal path. Total visible turns are `max_discussion_turns + participant_count + 2`. Existing `max_turns`, selected-runner accounting, and same-turn same-member `speaker_selected -> speech` validation remain separate. | `internal/storage/council_discussion_lifecycle.go`, `internal/storage/council_discussion_lifecycle_test.go`, `internal/storage/summary_turn_accounting.go`, `internal/storage/council.go`, status/export surfaces, focused storage tests | local storage/daemon regressions, full control `make test`, docs guardrails, plugin contract/core checks, official Red/Orange/Gray review, Gray remediation, and Blue synthesis evidence are required before commit; no live/prod/push claim |
| 4 | plugin | RUNFIX2-004 | Local plugin implementation proof: clean visible Discord transcript UX while preserving audit evidence. Plugin KAH run `run-20260620T121230Z-dea687353d54` adds local pure-renderer proof that visible messages use short headers such as `[ATN | T15/15]` and do not expose session id, role/color, `speaker_selected_event_id`, `speech_event_id`, or runner ids in the Discord body. | plugin `surface_rendering`, operator guide/bundled guidance, visible harness templates; control summary/audit compatibility as needed | local visible-vs-audit rendering tests; audit/export retains ids; no Discord lifecycle inference, live Discord delivery, production readiness, or pilot success claim |
| 5 | plugin | RUNFIX2-005 | Local plugin implementation proof: plugin KAH run `run-20260620T131542Z-ae344b0d5ee9` adds explicit-only `integrated_discussion_proof` planner/schema/test support for the hardened discussion runtime. The proof axes remain separated: lifecycle, selected-runner, participant runtime readiness at grant/turns, visible-surface, clean-transcript, visible-closeout, fallback, discussion-quality, and final labels. Selected-runner proof requires explicit runner success plus canonical linked speech for the selected member; manual/profile fallback is diagnostic-only and cannot repair selected-runner failure. | plugin activation planner/schema/tests, plugin operator evidence, control stream/status/export evidence as input only | local plugin proof only; `live_readiness` remains false; no live pilot success, production readiness, live Discord delivery, daemon/profile/provider/gateway/auth/token/model mutation, push, broad rollout, or control-owned implementation claim |

RUNFIX2 task 5 may report `selected_runner_pass: true`, `visible_surface_pass: true`, and participant runtime readiness success only when the event log shows the matching causal chains. Manual/profile fallback or profile CLI harness success may remain diagnostic, but it must not repair `selected_runner_pass` after a runner terminal failure.


## RUNFIX3 — Live-visible council contract hardening

`RUNFIX3` is the 2026-06-25 follow-up from 주유's KLM live Discord thread postmortem and 마초 Blue triage. The accepted SOT is `17thHermes:40_outputs/team/macho/atn/2026-06-25-atn-live-visible-council-contract-hardening-sot.md`. The failed run `sess_klm_selected_runner_20260625T085557Z` is diagnostic evidence only, not a successful live-visible council.

RUNFIX3 uses four PR-sized tasks. The pass labels remain evidence-derived and fail closed: no task may force lifecycle, selected-runner, visible-surface, origin-thread-binding, dialogue-mode, content/audit-separation, or discussion-quality success without matching durable evidence. The post-feedback execution-plan amendment freezes `selected_runner_pass` as an evidence-derived label and freezes the control evidence contract before plugin guidance/schema work starts.

RUNFIX3-wide completion requires the final cross-repo closeout artifact `40_outputs/team/macho/atn/<date>-runfix3-final-live-visible-council-closeout.md`, including exact live scenario, bound thread evidence, per-turn daemon/runner/speech/delivery linkage, negative fail-closed matrix results, Red/Orange/Gray final review, 마초 Blue synthesis, and 주군 approval.

| Global Order | Repo | Task ID | Scope | Suggested paths | Verification / gate |
|---:|---|---|---|---|---|
| 1 | cross-repo | RUNFIX3-001 | Completed/docs-only: lock this SOT and control/plugin roadmap entries for the 2026-06-25 live-visible council contract. | `17thHermes:40_outputs/team/macho/atn/2026-06-25-atn-live-visible-council-contract-hardening-sot.md`, control `docs/roadmap.md`, plugin `docs/roadmap.md` | docs diff checks, Red/Orange/Gray review, Blue synthesis; no runtime mutation |
| 2 | plugin | RUNFIX3-002 | implementation_complete/review_pending | Plugin-owned moderator/operator guidance hardening against the frozen control evidence contract is implemented and awaiting focused reviewer acceptance after cross-repo mirror/status reconciliation. Fixed minimum plugin gates passed; focused Red/Orange/Gray review and Blue synthesis remain pending acceptance. |
| 3 | plugin | RUNFIX3-003 | implementation_complete/review_pending | Plugin-owned planner/schema/operator-evidence support that consumes the frozen control evidence contract is implemented and awaiting focused reviewer acceptance. The plugin now carries exact-origin fail-closed proof, formula-grounded visible-turn accounting, start-gated daemon registry membership, and separate RUNFIX3 closeout/synthesis/delivery-target/prompt-envelope/dialogue/drift/fail-closed report axes without promoting live readiness. | plugin activation planner/schema/tests, operator evidence docs, positive/negative sample planner or final-report payload | implementation and fixed minimum test gates passed: `git diff --check`, `make docs-guardrails`, `make check-core-contract`, `make test-prepare`, completed `make test-unit` run as a deliberate tightening over the older waiver wording, focused `uv run python -m pytest tests/unit/test_discussion_activation_planner.py tests/unit/test_tool_schemas.py tests/unit/test_tool_handlers.py tests/unit/test_core_contract.py tests/unit/test_diagnostics_decoding.py tests/unit/test_surface_rendering.py`, and positive/negative sample planner or final-report payload. Pending review/acceptance gates: focused Red/Orange/Gray review and Blue synthesis. |
| 4 | control | RUNFIX3-004 | implementation_complete/review_pending: control diagnostics/enforcement implementation against the frozen evidence-label policy is under focused review. It now covers lifecycle accounting, selected-runner/delivery-evidence requirements, exact thread binding diagnostics, missing-stage preflight/status/finalization diagnostics, and fail-closed propose/finalize behavior with unresolved as the explicit terminal alternative. Durable verification evidence: `docs/evidence/2026-06-26-runfix3-004-diagnostics-evidence.md`. | control status/export/daemon/protocol diagnostics, lifecycle tests, selected-runner/delivery target tests, unresolved closeout diagnostics | fixed minimum gates passed: `git diff --check`, focused storage/daemon/protocol tests, `make docs-guardrails`, `make check-plugin-contract`, `make test-prepare`; focused Red/Orange/Gray review and Blue synthesis remain pending acceptance |

RUNFIX3 does not authorize production activation, live Discord rollout, daemon/profile/provider/gateway/auth/token/model mutation, package publication, push, live council runtime changes, or broad rollout.


## NEXFIX — Selected-runner prompt envelope remediation

`NEXFIX` is the 2026-06-26 follow-up from 주유's selected-runner prompt envelope defect report and the 2026-06-28 follow-up from Jooyoo's KLM live-visible selected-runner runtime delivery blocker. It is an explicit convention exception: the epic label is `NEXFIX`, while the task ids requested by 주군 use the `NEWFIX-*` prefix. SOT locks: `17thHermes:40_outputs/team/macho/atn/2026-06-26-atn-selected-runner-prompt-envelope-nexfix-sot.md` and `17thHermes:40_outputs/team/macho/atn/2026-06-28-atn-live-visible-selected-runner-runtime-delivery-newfix003-sot.md`.

Exit: selected-runner dispatch cannot produce generic canonical speech from an agenda-less content-plane prompt, cannot silently lose participant continuity when the selected member's own prior speech falls outside a tiny recent-context window, cannot start a Discord live-visible selected-runner council without an explicit live-visible timeout policy, and cannot let operator/plugin submission paths create an agenda that omits selected-runner mandatory context such as `success_criteria`. Control must construct selected-runner prompts from daemon projection, include selected-member own-history context, fail closed when mandatory context is unavailable, require/evidence `dispatch_timeout_sec=120` for Discord live-visible selected-runner councils unless an explicit approved alternative is recorded, and expose a CLI/conformance path for structured agenda required context. Plugin guidance/transport must make runtime readiness, content-plane readiness, prompt-continuity readiness, timeout readiness, and daemon command-id representation separate operator gates.

| Global Order | Repo | Task ID | Scope | Suggested paths | Verification / gate |
|---:|---|---|---|---|---|
| 1 | control | NEWFIX-001 | completed: projection-backed selected-runner prompt envelope is implemented and accepted after focused color review plus traceability repair. It includes `session_id`, `decision_question`, `success_criteria`, `out_of_scope_policy`, selected member id plus role/stance assignment, turn, causation id, required JSON schema, bounded prior speech/claim context, discussion-quality/ARGUE stance rule when prior claims exist, an explicit missing-context fail-closed instruction, and the `selected_runner_prompt_evidence` handoff contract; nil/empty prompt-builder output now fails closed before runner launch. | `internal/daemon/selected_speaker.go`, `internal/daemon/council_handlers.go`, projection/event-log helpers, selected-speaker tests, `docs/spec/live-transport-control-sot.md`, `docs/spec/operations.md` if prompt contract wording changes | verification passed: `git diff --check`, `go test ./internal/storage ./internal/daemon -run 'NEWFIX001|SelectedRunnerPrompt|SelectedSpeaker|RUNFIX009|RUNFIX011' -count=1`, `make test`; focused color review and Blue synthesis recorded in `/Users/draccoon/Workspace/SeventeenthEarth/agent-turn-network/feedback.md` |
| 2 | plugin | NEWFIX-002 | completed: plugin-packaged moderator/participant guidance for content-plane readiness preflight and missing-context diagnostics is implemented and accepted after focused color review plus traceability repair. | plugin `docs/spec/skill-and-operator-guide.md`, bundled `atn-moderator` and `atn-participant` skills, plugin live-transport SOT/roadmap mirrors | verification passed: `git diff --check`, `make test`; focused color review and Blue synthesis recorded in `/Users/draccoon/Workspace/SeventeenthEarth/agent-turn-network/feedback.md` |
| 3 | cross-repo | NEWFIX-003 | completed/docs-only: SOT lock and diagnosis for the 2026-06-28 live-visible selected-runner runtime delivery blocker. It records that the repaired KLM Discord-thread session still failed with `selected_runner_pass=false`, missing discussion turns, dispatch timeouts, visible delivery `context deadline exceeded`, a 30-second effective default when no session `dispatch_timeout_sec` is set, and prompt continuity limited to recent prior context. | 17thHermes SOT lock, control/plugin roadmaps, control/plugin live-transport SOTs, docs maps, timeout wording in control docs | docs diff checks, docs guardrails, stale status scan; no runtime mutation, live activation, or Discord delivery claim |
| 4 | control | NEWFIX-004 | implementation_complete/review_pending: selected-runner own-history prompt context is implemented and awaiting focused reviewer acceptance. Current scope keeps the reviewed global recent window at 6, adds all prior speeches by the selected member plus latest valid claims and fail-closed own claim-index reconstruction, records distinguishable own-history source ids, and adds round-robin continuity regression coverage. | `internal/storage/selected_runner_prompt_evidence.go`, storage/daemon prompt evidence tests, status/export evidence if shape changes, `docs/spec/live-transport-control-sot.md`, `docs/spec/operations.md` | fixed minimum gates passed: `git diff --check`, focused `go test ./internal/storage ./internal/daemon -run 'NEWFIX001|NEWFIX004|SelectedRunnerPrompt|SelectedSpeaker|RUNFIX009|RUNFIX011' -count=1`, and `make test`; focused re-review remains pending; no runtime mutation, live activation, or Discord delivery claim |
| 5 | control | NEWFIX-005 | implementation_complete/review_pending: Discord live-visible selected-runner timeout policy is implemented with explicit `dispatch_timeout_sec=120` or approved-alternative enforcement, typed `selected_runner_timeout_evidence` projection through council status and `bundle_manifest.json`, guarded runtime drift blocking, and `stream.status` intentionally unchanged. | `internal/daemon/council_handlers.go`, `internal/storage/session.go`, `internal/storage/council.go`, `internal/storage/transcript.go`, focused daemon/storage timeout evidence tests, `docs/spec/overview.md`, `docs/spec/protocol-and-cli.md`, `docs/spec/architecture.md`, `docs/spec/operations.md`, `docs/spec/live-transport-control-sot.md` | verification passed: `git diff --check`, `go test ./internal/storage -run 'NEWFIX005|RUNFIX014|RUNFIX3004|ARGUE004|SURFD002' -count=1`, `go test ./internal/daemon ./internal/storage -count=1`; focused re-review remains pending; no runtime mutation, live activation, or Discord delivery claim |
| 6 | plugin | NEWFIX-006 | implementation_complete/review_pending: plugin activation planner/operator guardrail now consumes `NEWFIX-004` own-history prompt evidence and `NEWFIX-005` timeout evidence, requires visible-surface readiness plus daemon registry membership before Discord-origin live-visible start-capable status, tightens approved-alternative timeout validation, and keeps local-only/artifact/manual bridge paths non-equivalent fallback. | plugin activation planner/schema/tests, `docs/spec/skill-and-operator-guide.md`, `docs/spec/live-transport-sot.md`, bundled `atn-moderator` and `atn-participant` skills | plugin docs guardrails, core contract/schema checks, positive sample for timeout 120 + own-history evidence, negative samples for missing timeout, missing own-history evidence, missing visible-surface or registry evidence, manual/output-mode fallback attempts, and approved-alternative mismatch/drift failures |
| 7 | control | NEWFIX-007 | completed: repaired the agenda-lock required-context submission contract so `atn-control council lock-agenda` can write structured `success_criteria`, CLI/SOT/conformance examples align with selected-runner mandatory prompt context, and agenda `--from-file` JSON behavior is explicit/fail-closed instead of silently storing a `draft` string. The daemon/storage boundary now requires `decision_question`, `success_criteria`, and `out_of_scope_policy` for direct `lock-agenda` commands. Request-feedback and handle-feedback gates are accepted after focused Red/Orange/Gray re-review plus Blue synthesis; MAR completed after the prior architecture status-surface coherence finding was remediated, focused arch re-review passed, and Blue disposition allowed KAH `mar_review` completion; second-color review has also passed and final verification and improve/lessons are complete. Second-color review, final verification, and improve/lessons gates are complete. This task does not run a live council. | `internal/command/daemn002.go`, `internal/storage/council.go`, `docs/spec/protocol-and-cli.md`, `docs/spec/protocol-and-cli.md`, `testdata/conformance/fixtures/command/council-lock-agenda-request.json`, `testdata/conformance/fixtures/event/agenda-locked-council.json`, command/daemon/storage tests, `.kkachi/runs/run-20260629T044729Z-fe990a0e0f92/mar-blue-disposition.md` | CLI regression for `--success-criteria`; JSON-file positive/negative behavior; storage-boundary negative tests for missing required agenda context; selected-runner missing-context negative test remains; conformance/plugin-contract checks; MAR artifacts, second-color acceptance, final verification commands, improve note, and KAH workflow completion evidence; no live activation claim |
| 8 | plugin | NEWFIX-008 | completed: plugin-owned live Unix socket `command.submit` representation now preserves structured `council.lock_agenda` agenda fields under daemon-owned `params.payload`, keeps daemon `payload.command_id` authority explicit, and fails closed before socket connect when caller-supplied `idempotency_key` cannot be represented as `payload.command_id` or `idem-{payload.command_id}`. The prior MAR arch control-mirror/status-surface blocker was remediated in control/plugin surfaces, focused arch re-review passed, and Blue MAR disposition accepted KAH `mar_review` completion; second-color review, final verification, and improve/lessons are complete. | `src/atn_plugin/client/transport.py`, `docs/spec/live-transport-sot.md`, `docs/spec/architecture.md`, live transport tests, plugin conformance/core-contract tests | focused live-transport tests for `council.lock_agenda` structured payload, idempotency boundary tests, plugin `make test-prepare`/unit/integration gates; no daemon authority, live readiness, Discord delivery, commit, or push claim |
| 9 | cross-repo | NEWFIX-009 | completed: focused cross-repo proof now covers the repaired agenda-context contract across control and plugin paths, and post-feedback remediation adds explicit empty/whitespace `success_criteria` fail-closed coverage plus restored run-root verification evidence. Control focused tests prove required agenda fields are present in `agenda_locked.payload`, included in `selected_runner_prompt_evidence.included_context`, linked by `agenda_source_event_ids`, and fail closed at the agenda storage/CLI boundary plus no-agenda selected-runner path with `selected_runner_dispatch_failed`. Plugin focused live-transport tests continue to prove structured `council.lock_agenda` payload preservation under daemon-owned `params.payload`. The cancelled `sess_council_1782671073534714000` remains diagnostic defect evidence, not success evidence. | control `internal/daemon/newfix001_selected_runner_prompt_test.go`, `internal/storage/council_test.go`, `internal/command/daemon_test.go`, plugin `tests/integration/test_live_unix_socket_transport.py`, run-local proof artifacts, control/plugin SOT mirrors | Red approved initial review; Orange/Gray request-changes were remediated; focused Orange/Gray re-review and Blue synthesis approved MAR entry; all five MAR roles are covered and Blue MAR disposition allowed KAH `mar_review` completion. Post-MAR second-color review, final verification, and improve/lessons are complete; live Discord rollout, broad readiness, commit, and push remain not claimed. |

NEXFIX does not authorize production activation, live Discord rollout, daemon/profile/provider/gateway/auth/token/model mutation, package publication, push, live council runtime changes, or broad rollout. A future plugin transport/schema hint for `agenda_event_id` or `prompt_context_hint` is out of scope unless 주군 separately approves it.


## COUNCIL-STAB — selected-runner council stabilization

`COUNCIL-STAB` records the 2026-06-30 follow-up from Jooyoo's KLM token/speed council blocker report. It is a cross-repo stabilization lane for the selected-runner golden path after local deterministic evidence showed that visible-looking council flow can still fail selected-runner completion if the floor grant lacks a daemon-authoritative stance source or if runnerless speech is counted as progress. SOT mirrors: control `docs/spec/live-transport-control-sot.md`, plugin `docs/spec/live-transport-sot.md`, plugin `docs/roadmap.md`, and `17thHermes:40_outputs/team/jooyoo/atn/2026-06-30-klm-token-speed-council-atn-blocker-report.md` as diagnostic source evidence only.

Exit: selected-runner floor grants must be backed by a matching same-member/same-turn `council.hand_raise` with non-empty `payload.intent` or `payload.reason`; control derives `speaker_selected.payload.stance_assignment` from that hand raise and ignores caller-supplied grant stance authority. Local proof must cover hand raise -> grant -> speaker_selected -> runner invocation -> linked canonical speech -> terminal closeout/status without allowing runnerless/manual speech or post-terminal diagnostic events to count as selected-runner success. Plugin ingress must fail closed before daemon submit when a selected-runner `council.hand_raise` lacks the required stance source.

| Global Order | Repo | Task ID | Scope | Suggested paths | Verification / gate |
|---:|---|---|---|---|---|
| 1 | cross-repo | COUNCIL-STAB-001 | completed/local-proof: cross-repo local stabilization is complete through the KAH improve/lessons gate. Red approved fail-closed/runtime-safety boundaries; Orange approved operator workflow with fake/local-vs-live wording carry-forward; Gray confirmed the prior status/SOT traceability blocker is remediated; Blue second-color synthesis allowed KAH to proceed to `final_verify`, final verification gates passed, and improve/lessons were recorded. | control `internal/storage/council.go`, `internal/storage/selected_runner_accounting.go`, `internal/daemon/council_selected_speaker_test.go`, `internal/command/daemn002.go`, `docs/spec/protocol-and-cli.md`, `docs/spec/protocol-and-cli.md`, `docs/spec/live-transport-control-sot.md`; plugin `src/atn_plugin/tools.py`, `tests/unit/test_tool_handlers.py`, `docs/spec/live-transport-sot.md`, `docs/roadmap.md`, bundled/operator guidance | fixed minimum local gates passed before official review: control focused storage/daemon/command tests, control `make test`, plugin focused tool-handler tests, plugin `make test`, docs/status sweeps. Official review: Red `t_543a1a62` APPROVE, Orange `t_7e1ac2c0` APPROVE with UX wording advisory, Gray `t_38b73a81` REQUEST_CHANGES for status/SOT traceability, Blue `t_8bab30af` REQUEST_CHANGES. Post-feedback remediation and MAR completed with Blue disposition; post-MAR second-color cards Red `t_9746263a`, Orange `t_3e11441a`, Gray `t_6fb1f968`, and Blue `t_e33a4835` approved proceeding to `final_verify`; final verification evidence is `.kkachi/runs/run-20260630T002950Z-fb8b14da44f1/verification.md`; improve evidence is `.kkachi/runs/run-20260630T002950Z-fb8b14da44f1/improvement-note.md`. No live Discord rerun, runtime activation, production readiness, push, profile/provider/gateway/auth/token mutation, or broad rollout is claimed. |

COUNCIL-STAB did not by itself authorize production activation, live Discord rollout or rerun, default Discord activation, daemon/profile/provider/gateway/auth/token/model mutation, package publication, push, live council runtime changes, or broad rollout. Historical failed sessions remain diagnostic evidence only until separately approved live evidence supplies fresh runner, canonical speech, and visible-delivery proof.


## LVCOR — Live-visible council one-pass reliability

`LVCOR` records Jooyoo's 2026-06-30 request-changes package for one-pass live-visible council operation. It is a five-task cross-repo epic with one global task number stream. The diagnostic source is `17thHermes:40_outputs/team/jooyoo/atn/2026-06-30-atn-developer-request-changes.md`.

Exit: the standard live-visible council path must compute the lifecycle shape from parameters, not from fixed turn labels. For `max_discussion_turns = n` and `participant_count = p`, the expected shape is T0 moderator opening, T1..Tn selected participant discussion turns, T(n+1)..T(n+p) participant closeouts, and T(n+p+1) moderator synthesis / terminal closeout. Expected visible turns are `n + p + 2`. Standard daemon/CLI/plugin/operator paths must not emit success-like completion unless this formula and terminal evidence pass, or the session is explicitly terminal-unresolved with exact blocker evidence. This epic did not by itself authorize production activation, live Discord rollout, default Discord activation, daemon/profile/provider/gateway/auth/token/model mutation, package publication, push, or broad rollout; the later default-setting update promotes Discord-origin decision-bearing councils to the standard live-visible lifecycle while keeping production readiness and runtime/profile/provider/gateway/auth/token mutation separately approval-gated.

| Global Order | Repo | Task ID | Scope | Suggested paths | Verification / gate |
|---:|---|---|---|---|---|
| 1 | control | LVCOR-001 | completed/control-local: KAH run `run-20260701T051848Z-90ca0c0febba` adds dynamic full-lifecycle accounting/status guard for parameterized live-visible councils. Status now separates discussion turns complete, participant closeouts complete, moderator opening/synthesis presence, terminal visible closeout proof status/blocker, terminal phase, and completion verdict; finalized reporting fails closed unless terminal visible closeout proof is `posted`. Official color review, MAR, post-MAR second color review, and Blue final synthesis accepted the control-local implementation with non-blocking carry-forwards only. | control `internal/storage/council_discussion_lifecycle.go`, `internal/storage/council_discussion_lifecycle_test.go`, `docs/spec/protocol-and-cli.md`, `docs/spec/protocol-and-cli.md`, `docs/spec/live-transport-control-sot.md` | GJC ultragoal `G001` completed with quality gate hash `79b4c521151e6b0824a1ddfebd7decc5498d4c4d2e56c39e88f1762e710cebba`; local gates passed: focused LVCOR/RUNFIX2/COUNCILSTAB storage tests, `go test ./internal/storage`, `git diff --check`, `make docs-guardrails`, `make check-plugin-contract`, `make test`, and KAT `test-prepare`/`test`/`docs-guardrails`/`check-plugin-contract`; MAR repaired/completed and post-MAR Red/Orange/Gray plus Blue synthesis accepted. Live Discord rerun, production activation, profile/provider/gateway/auth/token/model mutation, commit, push, and broad readiness remain unclaimed. |
| 2 | control | LVCOR-002 | completed/control-local: KAH run `run-20260701T093808Z-bfd2968703e2` makes terminal moderator synthesis/finalize proof parameterized and first-class instead of tied to a hard-coded T20 path. Derived status includes terminal synthesis turn/index/event/summary fields; standard live-visible `discord_thread` finalization now fails closed unless posted visible closeout proof has explicit `surface_evidence.status=posted`, configured-thread binding, and a visible-message pointer (`final_message_id`, `message_id`, `message_ref`, or explicitly typed equivalent). `kanban_comment_id`, `vault_decision_note`, and generic return evidence are linked-authority evidence only, not visible-surface proof. Red/Gray focused re-review accepted the REQUEST_CHANGES remediation; MAR recovery rerun covered logic, security, arch, cve, and test_adequacy with merge-pack PASS and Blue PASS_WITH_FINDINGS disposition. Official post-MAR Red/Orange/Gray review plus dependent Blue synthesis accepted the second-color gate, KAH `second-color-review` completed at workflow revision 37, and final KAH closeout completed at `evt-003521`, revision 39, with `final-report.md` plus `gate-reports/final.json`. `council.unresolved` remains the honest terminal alternative. | control `internal/storage/council_discussion_lifecycle.go`, `internal/storage/council.go`, `internal/command/daemn002.go`, `internal/storage/*lifecycle*_test.go`, `internal/storage/council_test.go`, `internal/command/daemon_test.go`, `docs/spec/protocol-and-cli.md`, `docs/spec/protocol-and-cli.md`, `docs/roadmap.md` | GJC ultragoal `G001` checkpoint completed after Blue reconciliation of a timed-out GJC executor attempt; focused storage/command LVCOR-002 tests passed; Red/Gray blocker regression added for missing/empty `surface_evidence.status` and linked-authority-only pointer classes. MAR recovery evidence: `artifacts/mar/recovery-20260702T071934Z/mar-run-summary-recovery.json`, `merge-pack-recovery.stdout.json`, and `mar-blue-disposition.md`. Second-color request: `second-color-review-request.md`; final gate, live Discord delivery, runtime activation, production readiness, profile/provider/gateway/auth/token/model mutation, commit, push, and broad rollout remain unclaimed. |
| 3 | control | LVCOR-003 | completed/control-local: KAH run `run-20260702T100541Z-519854f32aea` has implemented, verified, official-review accepted, feedback-handled, MAR-reviewed, post-MAR color-reviewed, and final-closed CLI/daemon schema discoverability and transport timeout semantics for the standard live-visible lifecycle; post-MAR official second-color review completed at KAH event `evt-003600`, workflow revision 37, and final KAH closeout completed at `evt-003602`, revision 39. Current artifacts expose canonical `--turn` help/discoverability, keep `--round` as a CLI compatibility alias only, add structured lifecycle `--from-file` schema diagnostics for `hand-raise`, `grant`, and `speak`, and separate accepted `council.grant` appends from selected-runner timeout/status follow-up. | control `internal/command/daemn002.go`, `internal/command/daemon_test.go`, `internal/daemon/council_handlers.go`, `internal/daemon/council_selected_speaker_test.go`, `internal/storage/selected_runner_accounting.go`, `docs/spec/protocol-and-cli.md`, `docs/spec/live-transport-control-sot.md`, plugin `docs/spec/live-transport-sot.md` mirror | focused Go, Make, docs, plugin contract, KAT, official review, feedback handling, MAR recovery merge-pack, post-MAR second-color review, final KAH closeout, and post-final docs/status checks have passed. Live Discord delivery, runtime activation, production readiness, profile/provider/gateway/auth/token/model mutation, commit, push, and broad rollout remain unclaimed. |
| 4 | plugin | LVCOR-004 | completed/plugin-local: plugin run `run-20260702T141926Z-a716fc43a7d2` and commit `1137c92` reconcile typed council command envelopes with daemon accepted payloads and add lifecycle golden tests while preserving daemon state authority. | plugin `atn_council_command` schemas/handlers/client transport tests, plugin `docs/spec/architecture.md`, plugin `docs/spec/live-transport-sot.md` | caller-supplied `command_id`, `request_id`, and `idempotency_key` are preserved; canonical turn-bearing envelopes preserve `payload.turn`; caller-supplied legacy nested `payload.payload.round`, missing `payload.turn`, and invalid turn values fail closed before transport; plugin owns no lifecycle/log/cursor/dedupe state; live Discord delivery, runtime activation, production readiness, profile/provider/gateway/auth/token/model mutation, push, release, and broad rollout remain unclaimed |
| 5 | cross-repo | LVCOR-005 | completed/plugin-local-final: plugin run `run-20260702T195344Z-341cf06c02d6` adds the parameterized one-pass full-shape proof surface and operator checklist consuming completed LVCOR-001 through LVCOR-004 fixes. Focused status-remediation re-review accepted the original stale-row blocker, strict MAR rerun passed with all required roles covered, KAH `mar-review` completed at `evt-003295`, and post-MAR second-color review completed at `evt-003302` with Blue `APPROVE_WITH_FINDINGS / ATN_BLUE_SECOND_COLOR_ACCEPTED_WITH_FINAL_STATUS_RECONCILIATION_REQUIRED`; final KAH closeout completed at `evt-003309`, workflow revision 41, with `final-report.md` and `gate-reports/final.json`. | plugin planner/operator guide/bundled `atn-moderator`, schema, tests, docs, cross-repo status mirrors | local verification covers 15 turns/4 participants -> 21 visible turns and 5 turns/2 participants -> 9 visible turns, rejects fixed-T20 assumptions for the 5/2 shape, keeps runnerless/manual selected turns at zero, requires participant closeouts and T(n+p+1) terminal synthesis, and separates finalized success from explicit unresolved blocker evidence. This row does not claim live Discord delivery, runtime activation, production readiness, profile/provider/gateway/auth/token/model mutation, commit, push, release, or broad rollout. |


## PRSLR — Persistent runtime session latency repair

`PRSLR` records the 2026-07-04 persistent participant runtime SOT: `17thHermes:40_outputs/team/macho/atn/2026-07-04-atn-persistent-participant-runtime-design-sot.md`. The target is not selected-only fan-out reduction. The target is council-scoped persistent participant actors: every public speech produces all-member cursor coverage, non-speakers receive observe deltas, the speaker receives authored canonical commit/self-ack, each member responds with `hand_raise` or `drop`, and the selected member resumes the same session for speech.

Target exit after PRSLR-007: the hot path is `delta` only; recovery is `rehydrate` only; stateless fallback and repeated full-history prompt stuffing are rejected. Rehydrate/observe/ack failures are runtime consistency failures, not `drop`, and default to whole participant runtime/council blocked unless a later explicit approval authorizes degraded-member continuation.

Operator acceptance summary:

| Task | Required operator/evidence proof |
|---|---|
| PRSLR-001 | Locks the seven-task sequence, owner boundaries, acceptance matrix, non-claims, and implementation gate after plan review. No runtime mutation. |
| PRSLR-002 | Proves first-class `council.drop` / `hand_raise_dropped`, rejects `wants_to_speak=false` in persistent mode, and covers forged/stale/duplicate/idempotency command cases. Freshness checks that require window/session/cursor state are integration-gated by PRSLR-003/004. |
| PRSLR-003 | Proves 120s bounded windows, all-members-responded early close, timeout auto-drop, late exclusion, deterministic timeout/late races, and restart replay without duplicate auto-drops. |
| PRSLR-004 | Proves per-council per-member session handle reuse, generation tracking, all-member cursor coverage, non-speaker observe delta, speaker authored self-ack, and same-session selected speech resume. |
| PRSLR-005 | Proves `participant_runtime_mode=persistent_delta`, delta-only hot prompts, omitted full-history lanes, compact rehydrate success, rehydrate failure fail-close, and `stateless_fallback=false`. |
| PRSLR-006 | Proves plugin schema/tool/operator guidance consumes daemon evidence and does not invent lifecycle/cursor/dedupe/fallback state. Fake/injected fixture proof is local only until control evidence exists. |
| PRSLR-007 | Re-runs the full SOT regression/evidence matrix across control and plugin, produces the operator report, updates docs/status, and keeps the result non-live/non-production. |

| Global Order | Repo | Task ID | Scope | Suggested paths | Verification / gate |
|---:|---|---|---|---|---|
| 1 | cross-repo | PRSLR-001 | Plan-vet and implementation contract lock. Confirm seven tasks, acceptance matrix, risk boundaries, Red/Orange/Gray plan review, Blue synthesis, and implementation authorization gate. | 17thHermes SOT, control/plugin roadmaps, docs maps, KAH/KAS plan artifacts | docs diff checks, CodeGraph/code-surface evidence, KAH/KAS plan-vet, official Red/Orange/Gray review; no runtime mutation |
| 2 | control | PRSLR-002 | First-class `council.drop` command/event and validation. Reject `wants_to_speak=false` as canonical drop in persistent mode, implement command schema/field capture plus actor/member/session/request_event_id/auto/idempotency validation, and add forged/stale/duplicate negative tests. Generation freshness, observed_cursor coverage, and latest-public-event checks are enforced against PRSLR-003/004 window/session/cursor state before final integration acceptance. | `internal/storage/council.go`, daemon council handlers, CLI/protocol/conformance fixtures, protocol docs | drop positive/negative tests, forged/stale command rejection, duplicate/idempotency tests, later integration coverage for generation/cursor freshness, docs/plugin-contract gates |
| 3 | control | PRSLR-003 | Response-window state, scheduler, and replay. Implement 120-second bounded windows, all-responded early close, timeout auto-drop, late response exclusion, atomic close/race behavior, and restart reconstruction. | storage window state, daemon scheduler/clock hooks, replay tests, status/export docs | simulated early close, timeout auto-drop, late-response race, partial restart/no duplicate auto-drop tests |
| 4 | control | PRSLR-004 | Persistent participant session registry and all-member observe coverage. Open/prewarm per-member sessions, persist handle/generation/last cursor, fan out observe deltas, speaker self-ack, and same-session selected speech resume. | `internal/memberruntime/`, `internal/daemon/runrt.go`, selected speaker dispatch, stream/cursor status, storage/session registry helpers | handle reuse proof, all-member cursor coverage, speaker authored commit/self-ack, same-session speech linkage, member lock ordering tests |
| 5 | control | PRSLR-005 | Delta prompt policy and rehydrate fail-close. Add `participant_runtime_mode=persistent_delta`, remove full-history hot lanes from delta prompts, build compact rehydrate snapshots, require rehydrate ack/ref/identity validation, and block on failure. | selected-runner prompt/evidence builders, rehydrate snapshot builder, status/export evidence, prompt tests | prompt negative tests for stale lanes, rehydrate success/failure tests, fallback branch absent/blocked tests, `persistent_participant_runtime_evidence` control output |
| 6 | plugin | PRSLR-006 | Plugin command/schema and evidence-surface alignment. Add `council.drop` typed tool schema support, consume control `persistent_participant_runtime_evidence`, and update planner/operator guidance without owning daemon lifecycle/cursor/dedupe/fallback state. | plugin schemas/tools/client tests, activation planner/operator docs, bundled guidance if scoped | fake/injected transport tests for drop, schema validation, planner evidence consumption, no plugin-owned lifecycle/fallback assertions |
| 7 | cross-repo | PRSLR-007 | Cross-repo acceptance proof and documentation closeout. Prove the SOT matrix across control and plugin, update docs/status surfaces, produce the operator report, and close with Red/Orange/Gray review plus Blue synthesis. | control/plugin test suites, conformance fixtures, docs/roadmaps/maps, 17thHermes closeout evidence | control Make/KAT gates, plugin Make/KAT gates, 20-regression SOT matrix, docs guardrails, review closeout; operator report must include readiness_label, evidence paths, mode=`delta|rehydrate`, participant session handle reuse refs or redacted hashes, omitted_full_history proof, stateless_fallback=false, all_member_cursor_coverage, response_window elapsed/closed_reason, auto_drop and late_ignored accounting, rehydrate_status/fail_close_event_id, blocked owner/next gate, and non-claims; no live readiness claim |

PRSLR does not authorize production activation, live Discord rollout, daemon/profile/provider/gateway/auth/token/model mutation, package publication, push, live council runtime changes, or broad rollout.

## ATN — Agent Turn Network public rename

`ATN` is the user-approved public rename task stream for Agent Turn Network. It is intentionally recorded as a three-letter epic exception because the product short name is the task id source of truth. ATN is a clean public rename with no in-repository legacy aliases.

| Global Order | Repo | Task ID | Scope | Suggested paths | Verification / gate |
|---:|---|---|---|---|---|
| 1 | cross-repo | ATN-001 | Completed/docs-only: locked control and plugin ATN naming SOTs, roadmap/index/docs-map entries, and no-alias policy after Red `t_d43402f0`, Orange `t_6d6bb8e8`, Gray `t_7ebc9e1e`, and Blue synthesis `t_8e348f72`. | `docs/spec/agent-turn-network-control-naming-sot.md`, plugin `docs/spec/agent-turn-network-plugin-naming-sot.md`, roadmaps, docs maps, docs indexes | docs guardrails, plugin contract/core checks, Red/Orange/Gray review, Blue synthesis; no binary/package/runtime mutation |
| 2 | control | ATN-002 | Completed/local-docs-proof: scrubbed control public docs, examples, protocol wording, roadmap/index/map surfaces, and operator-facing text to ATN-only wording ahead of ATN-004, updated sibling workspace path references to the current `agent-turn-network-plugin` checkout, and removed superseded prior-rename references from the checked-in docs set. | control README/docs/docs-map/roadmap/protocol/CLI docs | `git diff --check`, docs guardrails, plugin contract check, stale public-name scan |
| 3 | plugin | ATN-003 | Completed/local-docs-proof: rewrote plugin public docs, package/docs metadata, operator guide, bundled skill documentation, and local sibling workspace references to ATN-only wording ahead of ATN-005 with superseded prior-rename references removed from the checked-in docs set. | plugin README/docs/docs-map/operator guide/bundled skill docs | plugin `git diff --check`, docs guardrails, core-contract check, stale public-name scan |
| 4 | control | ATN-004 | Completed/local-proof: renamed control code, Go module, CLI binary, daemon binary, data-home/env/socket/protocol markers, daemon principal, conformance fixtures, tests, scripts, and Makefile surfaces to ATN names with no aliases. | `go.mod`, `cmd/`, `internal/`, `testdata/conformance/`, `scripts/`, `Makefile`, docs | focused Go tests, help smoke, conformance/contract checks, full control gates |
| 5 | plugin | ATN-005 | Completed/local-proof: renamed plugin package/import/manifest/tools/bundled skills to ATN names, updated no-alias guardrails, and closed final cross-repo ATN compatibility proof. | plugin `pyproject.toml`, `plugin.yaml`, `src/`, bundled skills, schemas/handlers/tests/scripts/docs | plugin-load smoke, tool/schema tests, docs guardrails, core-contract check, full plugin gates, cross-repo stale scan |
